# 設計書: CLI メッセージの英語対応（i18n）

対象: `novel_downloader.py`
方針の前提: `CLAUDE.md`「国際化（i18n）方針」— **ヘルプとエラーのみ英語対応**、青空文庫書式と縦組み組版は日本語固定。
目的: 日本語コンテンツを扱いたい**日本語学習者・非日本語環境のユーザー**が、失敗したときにメッセージを読めて検索できる状態にする。

> **進捗（2026-09-19）**: PR #1（PatchScratch 氏・squash `6b9483e`）のマージにより
> **Step 2b（P1 ヘルプ 39 件の英語化）と Step 3（言語解決）は完了済み**。
> `--lang {ja,en}` / `-L` と `NOVEL_DOWNLOADER_LANG` が実装され、`--lang en --help` の
> 日本語行は 0 行、日本語ヘルプは `--lang` の行が増えた以外は無変更（実測で確認）。
> `LANG` / `LC_ALL` は参照しない。**残るは Step 0 / Step P / Step 1 / Step 2a。**
>
> PR はヘルプの日英切り替えに `H(en, ja)` のインライン対を使っている。Step 1 の `T()`
> カタログと二本立てになるが、ヘルプは 1 関数に密集した対、エラーは 471 箇所に散在、と
> 性質が違うので併存は許容できる。統一するかは Step 1 の着手時に判断する。

---

## 1. 洗い出し結果

`ast` で `print()` / `sys.stderr.write()` / ロガー呼び出しを全走査し、日本語を含むものを抽出した（再現: `tools/extract_messages.py`）。

| 指標 | 件数 |
|---|---:|
| 日本語を出力する呼び出し箇所 | **471** |
| うちユニークな文言 | **229** |
| 1回しか出現しない文言 | 165 |
| CLI ヘルプ（`help` / `description` / `epilog`） | **39** |

### 1.1 出力種別

| 種別 | 箇所 |
|---|---:|
| `print()` | 457 |
| ロガー（`.warning` / `.error` / `.info`） | 14 |

### 1.2 優先度別（ユニーク文言ベース）

| 層 | ユニーク | 箇所 | 内容 |
|---|---:|---:|---|
| **P0 エラー・警告** | 95 | 129 | `エラー:` `[エラー]` `[警告]` `⚠` `〜できません` `〜が必要です` |
| **P1 ヘルプ** | 39 | 39 | `_build_arg_parser()` の `help` / `description` |
| **P2 進捗・情報** | 42 | 147 | `✅` `📖` `[n/3]` `[情報]` |
| **P3 データ出力・その他** | 92 | 195 | 作品情報表示、エピソード一覧、集計表示 |

**方針に沿った実装対象は P0 + P1 = ユニーク 134 件**。P2 / P3 は対象外（§4 の理由による）。

---

## 2. 重要な発見

### 2.1 呼び出し箇所の 49% は定型ブロックの複製

21 個の `run_*` 関数が、同じ意味の出力をそれぞれ独自に書いている。

| 定型ブロック | 箇所 | 出現関数数 | ユニーク文言 |
|---|---:|---:|---:|
| 3段階進捗 `[1/3]〜[3/3]` | 48 | 16 | 14 |
| 作品情報ヘッダ（タイトル・著者） | 43 | 21 | 12 |
| 件数表示（エピソード数・総文字数等） | 39 | 19 | 11 |
| 出力完了（`✅ テキスト/ePub出力完了`） | 36 | 19 | 3 |
| URL 正規化の3行 | 36 | 1 | 6 |
| `📖 ePub生成中...` | 17 | 17 | 1 |
| `[resume]` / `[Step N]` | 11 | 4 | 11 |
| サイト判別・作品トップ | 5 | 5 | 3 |
| **小計** | **235 (49%)** | | |

**同じ表示に表記ゆれがある。** 例（作品情報ヘッダのタイトル行だけで5種）:

```
'タイトル    :  {}'      'タイトル  :  {}'      'タイトル :  {}'
'タイトル:  {}'          '著者        :  {}'    '著者    :  {}'
```

翻訳前にこれを共通ヘルパーへ寄せると、**触る箇所が 471 → 約 244 に半減し、表記ゆれも同時に解消する**。i18n と independently に価値がある整理なので、先にやる。

### 2.2 【重大】GUI が CLI の標準出力をパースしている

`novel_downloader_gui.py` は子プロセスの stdout を正規表現で読んでいる。

```python
_RE_PROGRESS  = re.compile(r"^\s+\[\s*(\d+)\s*/\s*(\d+)\s*\]")
_RE_EPUB_DONE = re.compile(r"✅\s*ePub出力完了:\s*(.+)$")
```

**`✅ ePub出力完了:` は表示文言ではなくプロセス間の契約である。** 翻訳すると Windows GUI が完了を検出できなくなり、「終わったのに完了しない」という無言の故障になる。

（Android は `PROGRESS_CALLBACK` によるインプロセス連携なので影響を受けない。）

### 2.3 ただしイベント発火の単一挿入点は既に存在する

`_progress(n, total, title)` は **print をしていない**。`PROGRESS_CALLBACK` を呼ぶだけの3行の関数で、
**13箇所すべてで進捗 print と隣接して呼ばれている**（`CLAUDE.md` の新スクレイパー追加手順で規約化済み）。

```python
print(f"  [{ep_i:3d}/{len(target)}] {ep_title[:40]}")
_progress(ep_i, len(target), f"{ep_title[:40]}")
```

つまり**進捗イベントの発火点は `_progress()` ひとつで足りる**。同様に、出力完了イベントの発火点は
Step 0 が作る `_print_outputs()` ひとつになる。**§3 の Step 0 は protocol 移行のリスクではなく前提条件**である。

---

## 3. 実装案

### 3.0 全体の順序（確定）

```
PR #1 マージ
  └─ Step 0  定型ブロックの共通ヘルパー化（挙動非変更）
       └─ Step P  --progress-json（GUI 連携プロトコル）※正式な設計書は別途
            └─ Step 1  T() とカタログの骨組み
                 └─ Step 2a P0 エラー95
```

Step 2b（ヘルプ39）と Step 3（言語解決）は PR #1 で完了済み。

**Step 0 を protocol より先に置く。** §2.3 のとおり発火点を用意するのが Step 0 だからである。
Step 0 で `✅ ePub出力完了` の 18 箇所が1つのヘルパーに集まって初めて、
protocol はそこに1行足すだけで済む。順序を逆にすると、protocol 側が 18 箇所を個別に触ることになる。

### Step 0: 定型ブロックの共通ヘルパー化（i18n 前・挙動非変更）

`run_*` 21 個に散った定型出力を「共通ユーティリティ」セクションのヘルパーへ寄せる。

```python
def _print_work_info(title, author, *, original=None, episodes=None,
                     chapters=None, pages=None, chars=None): ...
def _print_stage(n, total, label): ...            # [1/3] 作品情報を取得中: ...
def _print_episode(i, total, title, suffix=""): ...  # ␣␣[  5/123] 第5話 ...
def _print_outputs(txt_path=None, epub_path=None): ...  # ✅ テキスト/ePub出力完了
def _print_normalized(before, after, kind="episode"): ...  # URL 正規化の3行
```

**確定事項:**

1. **表記ゆれは正準形へ統一する。** ラベルは全角4字分で右に `:` を置き、値まで半角スペース1つ。
   現状の `タイトル    :` / `タイトル  :` / `タイトル :` / `タイトル:`（4種）→ 1種に。著者6種 → 1種に。
2. **GUI 契約の2書式は互換性を壊さない**（§2.2）。許容される変更は書式ごとに異なる。
   - `  [N/M] {title}` — 13 箇所。**桁揃えの差（`:3d` / `:4d` / `:5d`）は統一してよい**。
     GUI 側は `^\s+\[\s*(\d+)\s*/` で、`[` 直後の空白を `\s*` が吸収するため右詰め幅に依存しない。
     正準形は `:4d` とする（4桁＝9999話まで整列し、実在作品を十分カバーする）。
   - `✅ ePub出力完了: {path}` — 18 箇所。**接頭辞・記号・コロン・空白すべて現状維持**。
     ヘルパーに寄せるだけで、出力バイト列は不変にする。
3. **ステージ見出し `[1/3]`〜`[3/3]` は自由に整形してよい。** GUI の正規表現は先頭空白を必須にして
   ステージ見出しを意図的に弾いている（`gui_v1_design.md` §9.2）。48 箇所・14 種の表記ゆれをここで解消する。
4. **`_progress()` には触らない。** Step P の発火点として現状のまま残す。
5. **P2 / P3 の文言も対象。** Step 0 は i18n ではなく重複排除なので、翻訳対象外の進捗・情報表示も含めて寄せる。

**検証**: 挙動非変更が要件。各サイト1作品を Step 0 前後で実行し、**stdout を diff して差分ゼロ**を確認する
（正準形へ統一した箇所のみ意図的差分として許容し、その一覧を PR 説明に残す）。

### Step P: `--progress-json`（GUI 連携プロトコル）

**正式な設計書は PR #1 マージ後、マージ後のコードベースに対して別途作成する。** ここでは方向のみ確定させる。

- stdout を JSON Lines のイベントストリームにし、人間向けログは stderr へ回す
- スキーマは `--detect-site` / `--list-sites` の既存規約（`{"schema":1,...}`）を踏襲する
- 発火点は `_progress()`（進捗）と `_print_outputs()`（出力完了）。Step 0 完了後は各1行で足りる
- **サブプロセス方式は維持する。** `gui_v1_design.md` §3 ①A の4理由のうち「クラッシュ非波及」
  「`.exe` 資産の分離」「中断の確実性」は現在も有効。失効したのは「本体無改修」のみ

### Step 1: メッセージカタログ方式

**採用案: 日本語原文をキーにする（gettext の msgid モデル）**

```python
def T(ja: str) -> str:
    """UI 言語に応じてメッセージを返す。未登録なら日本語をそのまま返す。"""
    return _MESSAGES_EN.get(ja, ja) if _UI_LANG == "en" else ja

_MESSAGES_EN = {
    "エラー: エピソード一覧を取得できませんでした。":
        "Error: could not retrieve the episode list.",
    "[警告] フォントファイルが見つかりません: {path}":
        "[warn] Font file not found: {path}",
}

print(T("エラー: エピソード一覧を取得できませんでした。"), file=sys.stderr)
print(T("[警告] フォントファイルが見つかりません: {path}").format(path=p), file=sys.stderr)
```

**この方式を選ぶ理由:**

| 論点 | 日本語原文キー（採用） | 記号キー `T("err.no_eps")` | インライン対 `L("ja","en")` |
|---|---|---|---|
| キーの考案 | 不要（229件分の命名が要らない） | 134個の命名が必要 | 不要 |
| 呼び出し側の可読性 | 日本語がその場に残る | カタログを見に行く必要 | 両言語が並び行が伸びる |
| 重複の集約 | 同一文言が自動で1エントリ | 手動 | 重複したまま |
| 未翻訳時の挙動 | **日本語にフォールバック** | KeyError の危険 | 起きない |
| 段階導入 | **可能**（P0 だけ先に訳せる） | 可能 | 全箇所改修が前提 |
| カバレッジ検査 | 機械的に可能 | 可能 | 困難 |

日本語フォールバックがあるため、**カタログが空でも動作は現状と完全に同一**。これが段階導入とリスク低減の要。

**確定事項:**

1. **プレースホルダは名前付き `{path}` を使う。** 位置指定 `{}` は英語で語順が変わったとき差し替え順を固定してしまう。
   `f"..."` は `T("...{x}").format(x=x)` へ書き換える。
2. **カタログは `novel_downloader.py` に埋め込む。** 単一ファイル配布（Windows exe・Android APK 同梱）が
   本ツールの前提であり、外部ファイル化すると同梱漏れで英語だけ消える事故が起きる。134 エントリ ≒ 270 行。
3. **`T()` を通すのは P0 + P1 のみ。** P2 / P3 は方針上の対象外（§1.2）。
4. **イベント（Step P）は翻訳しない。** `event` / `code` / `kind` は安定識別子として固定し、
   人間向けの `message` フィールドのみ `T()` を通す。GUI は `code` で分岐し、`message` は表示にのみ使う。
5. **エラーコードは 95 件すべてには振らない。** GUI が分岐に使う3種
   （`unsupported_site` / `needs_playwright` / `download_failed`、`gui_v1_design.md` §8 の①②③）に限る。
   残りは `code` なしの汎用エラーイベント＋ `message` で足りる。

### Step 2: 適用順序

1. **P0 エラー・警告 95 件** — 方針上の本丸。ここだけで「読めず検索もできない」は解消する
2. **P1 ヘルプ 39 件** — `_build_arg_parser()` に `T()` を通す。**全件同時に訳す**
   （一部だけだと日英混在ヘルプになり、全日本語より分かりにくい）
3. P2 / P3 は対象外

### Step 3: 言語解決

```
--lang {ja,en} / -L  >  NOVEL_DOWNLOADER_LANG  >  ja（既定）
```

- **`LANG` / `LC_ALL` は見ない。** `en_US.UTF-8` は WSL・Windows Python の一般的な既定値で、
  既存の日本語ユーザーの表示が無断で変わる
- パーサ構築より前に決まっている必要があるため、`argv` を直接先読みする
- 解決結果はモジュール変数 `_UI_LANG` に入れ、`T()` が参照する

---

## 4. 翻訳してはいけないもの（保護対象）

| 対象 | 理由 |
|---|---|
| `✅ ePub出力完了: {path}` | **GUI がパースする契約**（§2.2） |
| 進捗行 `  [N/M]` の書式 | 同上 |
| `--list-sites` / `--detect-site` の JSON | GUI 用の機械可読出力。`display_name` はサイト固有名詞 |
| `--list-only` のエピソード一覧 | 中身は作品タイトルそのもの |
| `.txt` 内の全ラベル（`底本URL：` 等） | パースキー。`CLAUDE.md`「対応しない層1」 |
| ePub 内の文言（奥付・目次・`第N話`） | 出力物の一部。`CLAUDE.md`「対応しない層1・2」 |
| サイト表示名（小説家になろう 等） | 固有名詞。`_SITE_COLOR_BY_LABEL` の照合キーでもある |

**P2 進捗を対象外にした理由は方針だけでなくこれ**。進捗表示には GUI 契約が混ざっており、一括で `T()` を通すと壊れる。

**Step P 完了後は上位2件が保護対象から外れる。** GUI がイベントの `event:"output"` を読むようになれば、
`✅ ePub出力完了:` は単なる表示文言に戻り、将来 P2 を訳す選択肢が開く。
残り5件（`.txt` ラベル・ePub 文言・JSON・サイト名）は方針上の恒久的な保護対象である。

---

## 5. 検証

`tools/check_i18n.py` を追加する（`tools/build_gaiji_table.py` と同じ位置づけ）。

1. AST で `T()` の第1引数リテラルを全収集
2. `_MESSAGES_EN` のキーと突き合わせ、**未翻訳**と**孤児エントリ**（原文が変わってキーが浮いた分）を報告
3. `{name}` プレースホルダが日英で一致するか検査（英訳での取りこぼしは実行時 `KeyError` になる）
4. 保護対象（§4）の文字列が `T()` に渡されていないか検査

```bash
python3 tools/check_i18n.py            # 未翻訳・孤児・プレースホルダ不一致を報告
python3 tools/check_i18n.py --stats    # 層別カバレッジ
```

手動確認:

```bash
python3 -m py_compile novel_downloader.py
python3 novel_downloader.py --lang en --help | grep -P '[\p{Hiragana}\p{Katakana}]'   # 0行になること
python3 novel_downloader.py --lang en https://ncode.syosetu.com/n0022gd/ --dry-run
LANG=en_US.UTF-8 python3 novel_downloader.py --help | head -3                         # 日本語のままであること
python3 novel_downloader_gui.py                                                       # 完了検出が効くこと
```

---

## 6. PR #1 との関係

https://github.com/ayati/novel_downloader/pull/1（PatchScratch 氏・英語 UI）は Step 1/2/3 と領域が重なる。

- **取り込める**: `--lang` の CLI 定義、GUI の言語切替と `UI` 辞書（GUI は独立しており本設計の対象外なのでそのまま活きる）、`README.en.md`、`.gitignore` の `android/.idea/`
- **本設計で差し替える**: `_resolve_ui_lang()` の `LANG` 参照を落とす（Step 3）。ヘルプは 39 件全件を `T()` 経由に統一する（PR #1 は3件のみ・インライン `if en else` 方式）
- **PR #1 に無い**: エラーメッセージ 95 件（本丸）、Step 0 のリファクタ、検証ツール

**2026-09-19 にマージ済み**（squash `6b9483e`）。指摘した3点はいずれも解消され、
`LANG` 不参照・ヘルプ全訳・日本語ヘルプの無変更を実測で確認した。

PR が触ったのは `_build_arg_parser()`（Step 2b）と `novel_downloader_gui.py` のみで、
**Step 0 が対象とする 471 箇所の `print` とは重ならなかった**ため、以降の順序は
**Step 0 → Step P → Step 1 → Step 2a**（§3.0）。

---

## 7. リスクと対策

| リスク | 対策 |
|---|---|
| GUI の完了検出が壊れる | §4 の保護対象を `check_i18n.py` で機械検査。GUI 実行を確認手順に含める |
| Step 0 のリファクタで出力が変わる | 挙動非変更のリファクタとして単独実施。各サイト実行し **stdout を diff して差分ゼロ**を確認（§3 Step 0） |
| Step 0 で GUI 契約の書式が変わる | 正準形統一の対象外として明示（§3 Step 0 確定事項2）。`tools/check_gui_contract.py` で機械検査 |
| Step P で GUI とエンジンの版がずれる | イベントに `schema` を持たせ、GUI は未知 schema を旧方式にフォールバック。正式設計書で詳細化 |
| 英訳漏れで日英混在 | 日本語フォールバックで壊れはしない。ヘルプのみ「全件揃うまでマージしない」 |
| 原文変更でカタログが浮く | `check_i18n.py` の孤児検査 |
| 単一ファイルが肥大 | カタログ ≒ 270 行。単一ファイル配布の利点が上回る |

---

## 8. 作業量の見積もり

| Step | 内容 | 規模 |
|---|---|---|
| 0 | 定型ブロック 8 種を共通ヘルパー化 | 21 関数・235 箇所 → 約 8 箇所 |
| P | `--progress-json` とイベント発火 | `_progress()` / `_print_outputs()` に各1行＋エミッタ約 60 行。GUI 側の読み取りループ改修。**正式設計書は別途** |
| 1 | `T()` と `_MESSAGES_EN` の骨組み、`_UI_LANG` 解決 | 新規 約 40 行 |
| 2a | P0 エラー・警告 95 件の `T()` 化と英訳 | 129 箇所 |
| ~~2b~~ | ~~P1 ヘルプ 39 件~~ | **PR #1 で完了** |
| 3 | `tools/check_i18n.py` | 新規 約 120 行 |
