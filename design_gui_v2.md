# novel_downloader GUI v2 設計書

> Windows 向け GUI（`novel_downloader_gui.py`）の改善設計。
> v1 設計書（`gui_v1_design.md`）が「含まない（将来検討）」に置いた項目を引き取り、
> あわせて v1 実装で見つかった不具合を潰す。
> 本書は v1 設計書を**置き換えない**。v1 で確定した方針（サブプロセス方式・CustomTkinter・
> 徹底ミニマル・既定保存先）はすべて維持し、その上に積む。

最終更新: 2026-09-22
状態: **v1.1 / v1.2 実装済み**（v1.3 以降は未着手）
対象: `novel_downloader_gui.py`（v1 実装・1007行） / `novel_downloader.py`（エンジン v2.11.2）

---

## 1. 経緯と問題意識

v1 は「**ePub をダウンロードしてエクスプローラで見られる**」という1点に絞って作られ、
その達成条件は満たしている。しかし実運用に入ると次の状態になっている。

- 作者自身が使っていない（CLI と `watcher.bat` で足りていたため）
- 外部から PR を受けて読み直したところ、**入力まわりが素通しだった**ことが判明。
  URL 欄は `Ctrl+V` でしか貼れない。右クリックメニューもペーストボタンもない
- エンジン（CLI）側は `--watch` / `--check-update-dir` / `--append-dir` / `--list-only` /
  `--dry-run` と機能が育っているのに、**GUI からは1つも使えない**
- `watcher.bat` / `watcherG.bat` が担っていた運用が GUI の外に取り残されている

v2 の目的は **「やっつけの皮」から「常用できる Windows アプリ」へ**。

---

## 2. 確定したユースケース（v2 の設計根拠）

v1 は「ブラウザで小説ページを開いている人」だけを想定していた。実際には**入口が2つ**ある。

### 入口A: PC で見つける（v1 が想定していた導線）

ブラウザで作品ページを開いている → URL をコピー → GUI に貼る → ダウンロード。

### 入口B: 出先でスマホで見つける（`watcher.bat` の実体・v1 は未対応）

> **Google Discover や X で、知らない Web 小説作品を出先で知る。
> その場で「あとで取る」と投げておき、帰宅後に PC で取得する。**

- **既知作品の更新チェックではない。** 新規作品の発見と確保が目的
- スマホからクラウド同期フォルダ（OneDrive / Google ドライブ）にテキストを落とす
- PC 側の `watcher.bat` がフォルダを 2 秒間隔でポーリングし、URL を抽出して
  `novel_downloader.exe` を起動、処理済みファイルを `done\` へ退避する

**この入口Bを GUI に取り込むことが v2 最大の追加価値**であり、
後述の「受信箱（インボックス）」として設計する。

### 2.1 入口Bが GUI 化で得るもの（`watcher.bat` の上位互換になる点）

`watcher.bat` は検知したら**無条件で即ダウンロード**する。これは
「知らない作品を投げておく」という用途と噛み合っていない。

| | `watcher.bat` | GUI 受信箱 |
|---|---|---|
| 取得前の情報 | なし（いきなり全話DL） | `--dry-run` で **タイトル・著者・全話数**を先に表示 |
| 取捨選択 | できない | 一覧で選んでから取得。不要なら捨てる |
| 重複 | 気づかず再取得 | 本棚に同URLがあれば「取得済み」と表示し、**更新確認**に化ける |
| 失敗時 | コンソールに流れて消える | 行に残り、再試行できる |
| 常駐 | 黒いコンソール窓が出っぱなし | タスクトレイ（v2.1） |
| 設定 | バッチを直接編集 | 設定画面 |

**「知らない作品」だからこそ、取る前に何なのか見たい。** ここが GUI 化の本質的な利点。

---

## 3. 現状の診断（v1 実装の不具合）

機能追加の前に潰す。いずれも v1.1 のスコープ。

### 3.1 【不具合】設定スキーマを上げると全設定が消える

```python
# novel_downloader_gui.py:234
if isinstance(data, dict) and data.get("schema") == SETTINGS_SCHEMA:
```

完全一致のため、**`SETTINGS_SCHEMA` を 2 に上げた瞬間に既存ユーザーの settings.json が
丸ごと無視され、保存先もフォントも無言で初期化される。**

- v2 では設定キーを増やすので、この修正を**最初に入れる**
- 読み込み側は `data.get("schema", 0) <= SETTINGS_SCHEMA` とし、欠損キーは既定で補完（現行どおり）
- 未知の新しい schema（将来版で作られた設定）は**読まずに既定で起動**し、上書き保存もしない
  （新しい版で追加された設定を古い版が消さないため）

### 3.2 【不具合】別作品の ePub を「完了」として開くことがある

```python
# novel_downloader_gui.py:845 _fallback_epub_path()
epubs = [os.path.join(d, f) for f in os.listdir(d) ...]
self._epub_path = max(epubs, key=os.path.getmtime)
```

出力先の**最新 .epub を無条件で採用**している。今回の実行で ePub が生成されなかった場合
（`output` イベントが来ない／ePub 生成前に失敗）、**前回の別作品の ePub を選択状態で開く**。

- ダウンロード開始時刻 `self._started_at` を記録し、`mtime < self._started_at` の
  ファイルは採用しない
- 該当なしのときは ePub を開かず、**出力フォルダだけ**を開く

### 3.3 失敗理由が1種類に潰れている

`_set_state_error()`（`:596`）のエラーは `unsupported` / `hameln` / `failed` の3種のみ。
ネットワーク断・作品削除・サイト構造変化・ディスク不足が**すべて「うまくいきませんでした」**になる。

- `_raw_log`（stderr 由来）から失敗理由の行を拾い、やさしい文言の下に小さく添える
- 生ログ全文は従来どおり「詳細を表示」の奥

> **【訂正・実装レビューで判明】目印は絵文字ではない。**
> 当初この節は「`❌` / `⚠` で始まる行」と書いていたが、**誤り**だった。
> `✅ ePub出力完了` があるので失敗側も絵文字だと思い込んだもので、実測すると：
>
> | 目印 | `novel_downloader.py` 内の出現数 |
> |---|---|
> | `❌` | **0 件**（存在しない） |
> | `⚠` | 4 件。すべて青空文庫**外字注記の非致命警告** |
>
> エンジンの実際の記法は **`エラー:` / `[エラー]` / `[警告]`**。
> さらに 404 などでは**エンジンが未捕捉例外で落ちる**ため、末尾はスタックトレースになる。
> 逆順に探すと `Traceback (most recent call last):` を拾ってしまい情報量ゼロだった。
>
> **確定仕様**（`_error_detail`）:
> 1. `_ERR_MARKS`（`エラー:` `[エラー]` `Error:` `error:`）で始まる行、または
>    `_RE_EXC_LINE`（`urllib.error.HTTPError: …` のような例外行）
> 2. 見つからなければ `_WARN_MARKS`（`[警告]` `⚠`）
> 3. それも無ければ空
>
> **警告を先に拾ってはいけない**（外字注記の ⚠ を失敗理由として表示してしまう）。

#### 【エンジン側で解決済み】未捕捉例外をやめ、1 行のエラーにする

上の「404 でスタックトレースになる」はエンジン側の積み残しだったので先に潰した。

- `main()` の直前に `_friendly_error(e)` / `_exit_fatal(e)` を追加。
  **文言は必ず `エラー: ` で始める**（GUI の `_ERR_MARKS` が拾う契約）
- 捕捉対象 `_FATAL_ERRORS = (HTTPError, URLError, RuntimeError, OSError)`。
  `HTTPError ⊂ URLError ⊂ OSError`・`requests.RequestException ⊂ OSError` なので
  通信系はこれで全部入る。**`TypeError` 等の素のバグは従来どおり落とす**（握り潰さない）
- 出し分け: 404 / 401・403 / 429 / 5xx / その他 HTTP / 接続不能 / ファイル I/O
- 送出位置は 2 箇所。`_main()` の `runner(args)` を包む方が本体で、
  `sys.exit(1)` に変換して既存の `except SystemExit` に落とすので
  **`--notify webhook` のエラー通知にもそのまま乗る**。`main()` 側は
  `--from-file` / `--from-epub` / `--watch` など dispatch を通らない経路の受け皿
- **調査手段は残す**。`NOVEL_DL_TRACEBACK=1` で従来どおり全トレースバックを出す。
  既定では代わりに `[情報] 詳細は NOVEL_DL_TRACEBACK=1 …` を 1 行添える
- 併せて `kky_fetch` / `alp_fetch` / `est_fetch` の
  `raise RuntimeError(f"URLの取得に失敗しました: {url}")` に最後の例外を付けた。
  これが無いと **404（作品削除）も接続断も同じ文面に潰れ**、GUI の失敗理由が
  「取得に失敗しました」だけになって役に立たない（他 14 サイトは元から付けていた）

| 入力 | 改修前 | 改修後（stderr 1 行目） |
|---|---|---|
| 存在しない ncode | Traceback 20 行 | `エラー: 作品が見つかりません（HTTP 404）。URLを確認してください: …` |
| 存在しないカクヨム作品 | Traceback 12 行 | `エラー: URLの取得に失敗しました: … — 404 Client Error: Not Found for url: …` |

終了コードはいずれも `1`（従来の未捕捉例外と同じ）。

### 3.4 ハーメルンを GUI が先回りして門前払いしている

```python
# novel_downloader_gui.py:684
if info.get("needs_playwright"):
    self._queue.put(("precheck", "hameln"))
    return
```

`--detect-site` の `needs_playwright` は「**このサイトは playwright を要する**」という
サイトの性質であって、「**この環境で動かない**」ではない。playwright を導入済みの環境でも
GUI が実行前に拒否してしまう。

- v1.1 では**止めない**。警告を出したうえで実行し、失敗したときに
  「ハーメルンには playwright が必要です」と案内する（`_set_state_error("hameln")` を事後へ移す）
- 「時間がかかる場合があります」の注意は実行前に1行出す

---

## 4. フェーズ計画

**先のフェーズに手を付けなくても、前のフェーズだけで完結して価値が出る**ように切る。

| 版 | 内容 | 画面構成 | 規模 |
|---|---|---|---|
| **v1.1** | §3 の不具合4件 ＋ 入力まわり（§5） ＋ stage 表示 | **変更なし**（bind とボタン追加のみ） | 半日〜1日 |
| **v1.2** | 結果まわり（§6）：エラー要約・ログ保存・ePubを開く・完了時挙動の設定化 | 変更なし | 1日 |
| **v1.3** | 複数URLキュー（§7） | URL欄を複数行化 | 1〜2日 |
| **v2.0** | **受信箱＋本棚**（§8） | **タブ構成へ再編** | 数日 |
| **v2.1** | 常駐・定期スキャン・通知（§9） | トレイ常駐 | — |

**v1.1 は既存の `_build_widgets()` に足すだけで、構造を一切触らない。**
まずここだけ入れて実機で触る。

---

## 5. v1.1 詳細設計（入力まわり）

### 5.1 右クリックのコンテキストメニュー

CustomTkinter に `Menu` 相当のウィジェットは無いので **`tkinter.Menu` を直接使う**。
`CTkEntry.bind()` は内部の `tkinter.Entry` に転送されるため、`self.ent_url.bind(...)` で足りる。

```python
def _attach_context_menu(self, widget):
    """入力欄に右クリックメニューを付ける（URL欄・保存先欄で共用）。"""
    import tkinter as tk
    menu = tk.Menu(self, tearoff=0)
    menu.add_command(label=..., command=lambda: widget.event_generate("<<Cut>>"))
    menu.add_command(label=..., command=lambda: widget.event_generate("<<Copy>>"))
    menu.add_command(label=..., command=lambda: widget.event_generate("<<Paste>>"))
    menu.add_separator()
    menu.add_command(label=..., command=lambda: widget.select_range(0, "end"))
    menu.add_command(label=..., command=lambda: widget.delete(0, "end"))
    widget.bind("<Button-3>", lambda e: self._popup_menu(menu, widget, e), add="+")
```

**決定事項:**

| 論点 | 採用 | 理由 |
|---|---|---|
| 実装 | `tkinter.Menu` | CTk に代替がない。OS ネイティブのメニューなので挙動も自然 |
| 操作の実体 | Tk 仮想イベント `<<Cut>>` / `<<Copy>>` / `<<Paste>>` | 自前で clipboard 操作すると選択範囲の置換・IME 中の挙動を取りこぼす |
| ボタン | `<Button-3>` のみ | Windows 専用アプリ。macOS の `<Button-2>` は対象外 |
| 適用先 | URL欄・保存先欄 | 共用関数にして両方に付ける |
| 項目の活殺 | ポップアップ直前に毎回判定 | 選択が無ければ「切り取り／コピー」を `disabled`、クリップボードが空なら「貼り付け」を `disabled` |
| 配色 | appearance mode に追従 | `tk.Menu` は CTk の外なので既定だとダークモードで浮く。`bg` / `fg` / `activebackground` を明示する |

**落とし穴:** `tk_popup` は `finally: menu.grab_release()` を必ず入れる。
握ったままだと以降のクリックを全部吸ってアプリが無反応になる。
ハンドラは `"break"` を返し、Tk 既定の選択移動と二重に動かさない。

### 5.2 キーバインド

| キー | 動作 | 備考 |
|---|---|---|
| `Ctrl+A` | URL 欄を全選択 | **Tk の既定は「行頭へ移動」で全選択にならない。** 明示 bind が必須 |
| `Enter` | ダウンロード開始 | 実行中（`self._proc is not None`）は**無視**する。「中止」が暴発するため |
| `Esc` | 実行中なら中止 | 確認なしで止まるのは v1 と同じ挙動 |
| `Ctrl+V` | 貼り付け（Tk 既定のまま） | 貼り付け後に §5.5 の判定を走らせるため `<<Paste>>` を監視する |

### 5.3 貼り付けボタン

URL 欄の右に 2 つ置く。

```
  小説のURLを貼り付け
  [ ____________________________________ ] [📋] [📋⬇]
  ✓ 小説家になろう                             ← §5.5 のバッジ
```

| ボタン | 動作 |
|---|---|
| `📋`（貼り付け） | クリップボードを URL 欄へ入れ、サイト判定を走らせる。**既存の内容は置換**する |
| `📋⬇`（貼り付けてダウンロード） | 上に加えてそのまま開始。ブラウザ→アプリ→**1クリック**で完結 |

- クリップボードが `http://` / `https://` で始まらない場合は、**入れずにバッジに理由を出す**
  （「クリップボードに URL がありません」）。黙って何も起きないのが一番わかりにくい
- `clipboard_get()` は選択が無いと `TclError` を投げるので必ず try/except

### 5.4 【最重要】ウィンドウがフォーカスを得たらクリップボードを再チェック

v1 の `_start_clipboard_autofill()` は**起動時の1回だけ**（`:339`）。
つまり「アプリを起動したまま、ブラウザで次の URL をコピーする」という最も自然な使い方で
自動入力が効かない。**ここを直すと §5.1〜5.3 は保険に格下げされる。**

```python
self.bind("<FocusIn>", self._on_focus_in, add="+")
```

**判定規則（この順に全部満たしたときだけ自動入力する）:**

1. `event.widget is self` — **`<FocusIn>` は子ウィジェットでも発火する。**
   ガードしないと入力欄をクリックするたびに走る（必須）
2. 実行中（`self._proc is not None`）でない
3. クリップボードが `http(s)://` で始まる
4. その文字列が**前回チェックしたものと違う**（`self._last_clip`）。同じなら無視
5. URL 欄が**空**、または**直前にダウンロードした URL のまま**
   → ユーザーが手入力・編集中の文字列は絶対に消さない
6. `--detect-site` で対応サイトと判定できた（別スレッド）

**上書きせず「候補」として出す案も検討したが採らない。**
判定条件 5 で「ユーザーの入力を消さない」ことが保証されるため、直接入れてよい。
確認 UI を挟むと 1 クリック増えて、v1 の「ワンクリック」方針に反する。

### 5.5 サイト判定バッジ（入力中の即時フィードバック）

v1 は**ボタンを押してから exe を起動して初めて**「このサイトには対応していません」と言う。
exe 起動は 1〜数秒かかるため、押してから無反応の時間が生まれる。

- URL 欄の変更後 **400ms 入力が止まったら** `--detect-site` を別スレッドで実行（デバウンス）
- `after_cancel` で前の予約を取り消す
- **世代カウンタ（seq）を持つ。** 遅れて返ってきた古い結果が新しい入力の判定を
  上書きしないようにする（必須）
- 結果はキャッシュし、`_download_worker_inner` の事前 `detect_site` 呼び出しを**省く**

| 状態 | 表示 | 大ボタン |
|---|---|---|
| 空 | （非表示） | 無効 |
| 判定中 | `判定中…` | 有効 |
| 対応サイト | `✓ 小説家になろう` | 有効 |
| 未対応 | `✗ このサイトには対応していません` ＋「対応サイトを見る」 | **無効** |
| playwright 必要 | `⚠ ハーメルン（playwright が必要・時間がかかります）` | 有効（§3.4） |

### 5.6 実行中も URL 欄を触れるようにする

```python
# novel_downloader_gui.py:575
self.ent_url.configure(state="disabled")
```

`disabled` のため **DL 中に次の URL を貼って待機させることができず、コピーすらできない。**
`readonly` に変更する（コピー・選択は可、編集は不可）。
v1.3 のキュー（§7）が入れば、ここは「キューに追加」に発展する。

### 5.7 stage イベントを表示に使う（実装コストほぼゼロ）

エンジンは `{"schema":1,"event":"stage","n":1,"total":3,"label":"作品情報を取得中: …"}` を
**既に送っているのに、GUI は捨てている**（`_read_events()` `:757` のコメント）。

```python
elif kind == "stage":
    self._queue.put(("stage", int(ev.get("n", 0)), int(ev.get("total", 3))))
```

**`label` は使わない。** `_print_stage()` の呼び出し側（`novel_downloader.py:4730` 他）は
**日本語ハードコード**で i18n されていないため、英語 UI に日本語が出てしまう。
`n` だけを使い、GUI 側の対訳表に差し替える。

| n | ja | en |
|---|---|---|
| 1 | 作品情報を取得中… | Fetching work info… |
| 2 | エピソード一覧を取得中… | Fetching episode list… |
| 3 | 本文を取得中… | Downloading episodes… |

`label` の全文は従来どおり stderr 経由で生ログに残る。

#### 【実装時の発見】`run_narou` だけ stage イベントを出さない → **解決済み**

実装後に全スクレイパーを確認したところ、**`_print_stage()` を呼んでいるのは 16 本で、
`run_narou` だけが独自の `[Step N]` 書式で `print()` している**（`novel_downloader.py`
の `run_narou` 内）。したがって**最もよく使われるサイトで段階表示が出なかった**。

- GUI 側は stage が来なければ v1 と同じ（「準備中…」→ 進捗行）に落ちるだけなので**後退はない**
- 当初は「`[Step N]` を `_print_stage()` に置き換える」案だったが、これは
  **CLI 既定の標準出力が変わる**（`[Step 1]` → `[1/3]`）ため
  `design_progress_json.md` §5 の「4 が最重要（既定の挙動を変えない）」に抵触する

**確定仕様（別途判断の結論）**: 表示と通知を分離する。`_print_stage()` から
`_emit_stage(n, label, total=3)` を切り出し、`run_narou` は
**`[Step N]` の print をそのまま残したまま `_emit_stage()` だけを呼ぶ**。

| 送出点 | `n` | ラベル |
|---|---|---|
| `run_narou` の `[Step 1]` 直前 | 1 | `作品情報を取得中: {base_url}` |
| `narou_get_all_episodes` の目次ループ直前 | 2 | `エピソード一覧を取得中...` |
| 本文取得ループ直前（resume 分岐の後・1 箇所） | 3 | `各エピソードを取得中...` |

段階 2 を `narou_get_all_episodes` の中に置いたのは、なろうが作品情報と目次を
1 関数で取るため `run_narou` からは境目に手が届かないから。`--check-update` 経由の
呼び出しでも送出されるが、`n` しか使わない GUI には無害。

- 検証: `--end 3 --no-epub` の全出力を改修前後で `diff` して**バイト一致**を確認済み
- 同様に `run_from_file` / `run_from_epub` / `run_watch` も stage を出さない（こちらは
  段階が 3 つに分かれないので問題ない・現状のまま）

### 5.8 追加する UI 文言（`UI` dict・ja / en 両方必須）

`novel_downloader_gui.py:60` の `UI` は ja/en のタプル。**片方だけ足すと英語 UI が壊れる。**

| キー | ja | en |
|---|---|---|
| `ctx_cut` | 切り取り | Cut |
| `ctx_copy` | コピー | Copy |
| `ctx_paste` | 貼り付け | Paste |
| `ctx_selectall` | すべて選択 | Select All |
| `ctx_clear` | クリア | Clear |
| `btn_paste` | 貼り付け | Paste |
| `btn_paste_dl` | 貼り付けてダウンロード | Paste & Download |
| `clip_empty` | クリップボードに URL がありません | No URL in clipboard |
| `detecting` | 判定中… | Checking… |
| `site_ok` | ✓ {name} | ✓ {name} |
| `site_ng` | ✗ このサイトには対応していません | ✗ This site is not supported |
| `site_pw` | ⚠ {name}（playwright が必要・時間がかかります） | ⚠ {name} (needs playwright; slow) |
| `stage1` / `stage2` / `stage3` | §5.7 の表 | 〃 |

### 5.9 追加する設定キー

§3.1 の修正を**先に**入れること。キーの追加だけなら `SETTINGS_SCHEMA` は 1 のまま据え置ける
（欠損キーは既定で補完されるため）。

| キー | 既定 | 用途 | 導入 |
|---|---|---|---|
| `auto_paste` | `true` | フォーカス時のクリップボード自動入力（§5.4）を切る逃げ道 | v1.1 |
| `open_folder_on_done` | `true` | 完了時のフォルダ自動オープン（§6） | v1.2 |
| `window_geometry` | `""` | ウィンドウ位置・サイズの記憶（v1 設計 §13.5 の見送り分） | v1.2 |

### 5.10 v1.1 の確認手順

```
 1. URL欄で右クリック → メニューが出る／貼り付けできる／ダークモードで色が浮かない
 2. Ctrl+A で全選択される（行頭移動にならない）
 3. Enter でダウンロードが始まる。実行中の Enter で中止が暴発しない
 4. 📋⬇ で貼り付け〜完了まで1クリック
 5. アプリを開いたままブラウザで別URLをコピー → アプリに戻ると自動で入る
 6. URL欄を手で編集した状態でアプリに戻る → 入力が消えない（判定条件5）
 7. 入力欄に子ウィジェットがある状態でクリックしても自動入力が暴発しない（判定条件1）
 8. 未対応URLを入れるとボタンが押せずバッジに理由が出る（exe 起動前）
 9. 進捗が「作品情報を取得中…→ エピソード一覧を取得中…→ 第N話/全M話」と進む
10. 英語 UI に切り替えても日本語が混ざらない（stage ラベル・メニュー）
11. DL中に URL 欄のテキストをコピーできる（readonly）
12. 既存の settings.json が初期化されない（§3.1）
13. ダウンロードに失敗したとき、前回の別作品の ePub が開かない（§3.2）
14. CLI 単体の挙動が 1 バイトも変わらない（GUI 変更のみでエンジン無改修）
```

---

## 6. v1.2 設計（結果まわり）

| 項目 | 内容 |
|---|---|
| エラー要約 | §3.3。`❌`/`⚠` の最終行をやさしい文言の下に小さく添える |
| ログ保存 | `📄 ログを保存` ボタン。`_raw_log` はメモリのみで**不具合報告に使えない**のが現状 |
| `📖 ePubを開く` | 「フォルダを開く」の先がゴール。`os.startfile(epub)`。将来 `yomikake.html` 連携 |
| 完了時の自動オープン | `open_folder_on_done` で切れるように（`:835`）。連続DLでエクスプローラが次々開く |
| 残り時間の目安 | `delay × 残り話数`。300話の待ち時間が読めないのは不安になる |
| バージョン表示 | GUI とエンジンの版ずれフォールバック（`:701`）まで書いてあるのに、**どの版が動いているか画面から分からない**。`--version` を叩いて about に出す |
| ウィンドウサイズ | `geometry("560x420")` / `("560x720")` のハードコード（`:884`）をやめ、記憶する。**§6.1 の DPI 罠に注意** |
| 詳細ログのサイズ | ログ行に `weight=1` を与えて窓の余白を吸わせ、下端のグリップでドラッグ変更できるようにする（§6.2） |

### 6.1 【実装時の発見】`winfo_*` と `geometry()` を混ぜてはいけない

相対リサイズを `winfo_width()` / `winfo_height()` を基準に実装したところ、
**150% 表示の Windows で詳細設定を開閉するたびにウィンドウが際限なく巨大化した。**

原因は CustomTkinter の単位系の二重構造：

| API | 返す／受け取る単位 |
|---|---|
| `CTk.geometry("WxH")`（設定） | **論理値**。内部で `× window_scaling` して Tk に渡す |
| `CTk.geometry()`（取得） | **論理値**（`_reverse_geometry_scaling` 済み） |
| `winfo_width()` / `winfo_height()` | **実ピクセル**（＝論理値 × scaling） |
| `event.x_root` / `event.y_root` | **実ピクセル** |

`winfo_height()` の値を `geometry()` に渡すと**倍率が二重にかかる**。
実測（倍率 1.5・論理 560×420）:

```
geometry()          → 560x420        ← 論理値
winfo_width/height  → 840x630        ← 実ピクセル
旧コードの要求値    → 1260x1395      ← 1 回の開閉で約 1.5 倍に膨らむ
```

開閉のたびに 1.5 倍が積み重なるため、数回で画面を超える。
**倍率 1.0 の環境（WSLg・等倍の Windows）では再現しない**ので、自動テストでも見つからなかった。

**規約:**
- ウィンドウサイズの読み取りは `_logical_size()`（引数なし `geometry()` をパース）に統一する。
  **`winfo_width()` / `winfo_height()` をウィンドウサイズ計算に使わない**
- マウス座標（`event.y_root`）は実ピクセルなので、`geometry()` に反映する前に
  `_window_scale()`（`ctk.ScalingTracker.get_window_scaling`）で割る
- 同じ罠は `minsize` / `maxsize` / `wm_geometry` にもある

### 6.1b 【重要】ウィンドウ高さを固定値で増減してはいけない

§6.1 で「`winfo_*` と `geometry()` を混ぜない」を決めたが、**もう一段の誤り**が残っていた。
パネル（詳細設定・ログ・一覧）の開閉で高さを**固定値**（詳細 +300px 等）で
増減していたため、パネルに項目を足すたびに値が実態とずれていく。

実機で「詳細設定を開いても中身が全部見えず、スクロールもできない」と報告された。
計測すると **v1.2 の時点で既に破綻していた**:

| 状態 | 窓 | 内容が要る高さ | はみ出し |
|---|---|---|---|
| 詳細設定だけ | 720 | 800 | **80px** |
| 一覧＋詳細 | 890 | 1052 | **162px** |
| 一覧＋詳細＋ログ | 1080 | 1192 | **112px** |

`DETAIL_DELTA_PX = 300` は v1 の値。v1.2 で「動作」グループ（チェックボックス2つ）を
足したときに据え置いたため、**その時点で詳細設定だけで 80px はみ出していた**。
キュー一覧はそれを悪化させただけで、原因ではない。

**確定仕様（`_fit_window`）**:

```python
need   = winfo_reqheight() / 倍率        # 内容が必要とする高さ（論理値）
target = max(h, need) if grow_only else max(need, restore_to)
target = clamp(target, BASE_HEIGHT_PX, 画面の高さ)
```

- パネルを**開く**とき `grow_only=True`（ユーザーが手で広げた分を縮めない）
- パネルを**閉じる**とき `restore_to=開く直前の高さ`
  （手で設定した大きさを開閉で失わない。内容に足りなければ内容側が優先）
- `BASE_HEIGHT_PX`(420) を下限にする。内容だけに合わせると 269px まで縮み、
  開閉のたびに窓が初期サイズより小さくなって落ち着かない
- **`geometry()` で読み直さない。** 設定した直後は WM が反映するまで古い値が
  返るため、基準高さが 1 世代ずれる（実際に `_base_height` がずれた）

**終了時に保存する高さ**も固定値の引き算をやめ、
「パネルを何も開いていないときの高さ」を `_base_height` に記録して使う。

**規約: ウィジェットを増やしたら高さの定数を直す、という運用に依存しない。
内容の実寸（`winfo_reqheight`）に合わせる。**

**副作用**: ログ欄の既定の高さを 120 → 180px にした。内容フィットでは
この値がそのまま「ログの見える量」になり、120 では初期ウィンドウの余白に
収まってしまって開いても窓が広がらず、以前より狭くなるため。

### 6.1c 詳細設定パネルをスクロール可能にする

§6.1b の内容フィットだけでは足りなかった。**ウィンドウは画面の高さが上限**なので、
そこで頭打ちになると、はみ出した分を見る手段が無い。実機でも
「詳細設定を開いても下の項目（横書き・Kobo・フォント・取得間隔・文字コード）に
到達できない」状態が残った。

**`frm_detail` を `CTkScrollableFrame` にした。** これで画面がどれだけ低くても、
パネル内をスクロールすれば全設定に届く。

`CTkScrollableFrame` の扱い（実装時に確認した事実）:

| 何を | どうする |
|---|---|
| 子ウィジェットの親 | そのまま `CTkScrollableFrame` を渡す（自身が内側の内容フレーム） |
| 中身の自然な高さ | `winfo_reqheight()`（自身が内容フレームなので中身の高さが返る） |
| **見える**高さ | `configure(height=N)`（外側フレームに反映される） |
| 配置 | `grid()` / `grid_forget()` / `grid_remove()` は外側フレームへ転送される |

**伸縮するパネルは 2 つある**（詳細設定・一覧）ので、`_fit_flexible_panels()` で
**まとめて配分する**。片方ずつ決めると、もう片方の存在を無視して画面からはみ出す。

```
avail = 画面の高さ − パネル以外に要る高さ − 余白
一覧  = min(自然, max(快適下限90,  avail − 詳細快適下限120))
詳細  = min(自然, max(快適下限120, avail − 一覧))
入らなければ絶対最小（詳細60 / 一覧40）まで詰めて分け合う
```

**下限を使える高さより優先してはいけない**（優先した結果はみ出した）。
「快適な下限」と「絶対最小」の 2 段構えにして、入らないときは後者まで詰める。

実測（論理値）:

| 画面上限 | 詳細のみ | 一覧＋詳細 |
|---|---|---|
| 2080 | 窓812 / パネル519（全部見える） | 窓1064 / 詳細519・一覧150 |
| 1000 | 窓812 / パネル483 | 窓812 / 詳細120・一覧111 |
| 768 | 窓768 / パネル251 | 窓768 / 詳細60・一覧40 |
| 640 | 窓640 / パネル123 | 45px はみ出す（下記） |

**残る制約**: 画面の高さが論理 700px 未満で**一覧と詳細を同時に開く**と、
固定部分（URL 欄・大ボタン・進捗・保存先など計 645px）だけで埋まり、
窓が画面より 45〜105px 高くなる。ただし**どちらのパネルもスクロールする**ので、
設定にも一覧にも到達はできる。使わないパネルを閉じれば解消する。

### 6.2 詳細ログのサイズ変更

v1 のログ欄は `height=120` の固定で `sticky="ew"` だったため、
**ウィンドウをいくら大きくしてもログ欄は 120px のまま**で、窓の下半分が空くだけだった。

- ログを開いている間だけ `grid_rowconfigure(<ログ行>, weight=1)` を与え、`sticky="nsew"` にする
  → ウィンドウの余った高さをログ欄が吸う
- ログ欄の下に 8px のグリップ（`cursor="sb_v_double_arrow"`）を置き、
  ドラッグでウィンドウ高さを変える。`weight=1` があるので**増えた分はそのままログ欄になる**
- 開閉時は `LOG_DELTA_PX`(190) ぶん窓を伸縮させる
- 下限は `MIN_HEIGHT_PX`、上限は画面の高さ（いずれも論理値）。
  なお**内容が必要とする高さより小さくはできない**（倍率が高いと下限に届かないことがあるが正常）

### 6.3 終了時に予約済み `after` を止める

`destroy()` 後に `after` コールバックが発火すると
`invalid command name "..._poll_queue"` が stderr に出る。
`_on_close` で `_poll_after` / `_detect_after` を `after_cancel` し、
`_poll_queue` は `self._closing` を見て即 return する。

### 6.4 【セキュリティ】`_open_epub` は拡張子を必ず確かめる

`📖 ePubを開く` の追加により、**`self._epub_path` が「表示するだけの値」から
`os.startfile()` に渡る実行シンクに変わった**。`os.startfile` は関連付けに従って
何でも起動するため、ここは扱いが変わる。

`_epub_path` には 2 つの入力経路がある：

1. stdout の JSON イベント（`output` / `kind:"epub"`）— エンジンが自分で組んだパス
2. **stderr を `_RE_EPUB_DONE`（`✅\s*ePub出力完了:\s*(.+)$`）で拾う経路** —
   `design_progress_json.md` §4.1 の版ずれ対策として意図的に残しているもの

経路 2 が問題で、`--progress-json` 指定時はエンジンが `sys.stdout = sys.stderr`
（`novel_downloader.py:11671`）とするため、**サイト由来の作品名・話タイトルが
そのまま stderr に流れ込む**。`\s*` と `.search()` のゆるさもあり、作品名で
この行を偽装できる。

**現時点では悪用できない**（偽装行が `finished` の直前に来ないことと、
`run_narou` が URL に `/` を付け足すことで `os.path.isfile()` が落ちるため）。
しかし**どちらも GUI が所有していないエンジン側の都合**であり、将来の変更で
静かに崩れる。GUI 側で断つ：

```python
if not (path and os.path.isfile(path) and path.lower().endswith(_EPUB_EXTS)):
    self._open_folder()      # 実行はせず、フォルダを開くに退避
    return
```

**規約: `os.startfile()` / `xdg-open` にパスを渡す前に、必ず拡張子を確かめる。**

---

## 7. v1.3 設計（複数URLキュー）

状態: **実装完了（Step 1〜4）**。実機 Windows で確認済み

### 7.1 これが解く問題

1. **新刊をまとめて取りたい。** 5 本あると今は 5 回、1 本ずつ終わるのを待って操作する
2. **実行中に次の URL を積めない。** v1.1 で URL 欄は `readonly` にしたが、
   ダウンロード中に見つけた作品はメモしておくしかない
3. **§8（受信箱・本棚）の実行基盤。** 「受信箱の 3 件をまとめて取得」も
   「本棚の新着 4 作品をまとめて追記」も、順番に実行して結果をまとめる仕組みは同じ。
   **v2.0 の前にここを作る**のはそのため

### 7.2 【方針転換】入力欄は 1 行のまま維持する

当初この節は「URL 欄を複数行（`CTkTextbox`）にする」と書いていたが、**採らない。**

v1.1 で作った入力まわり — プレースホルダ、サイト判定バッジ、`📋⬇ 貼り付けてダウンロード`、
`Enter` で開始、右クリックメニュー — は**すべて 1 行の `CTkEntry` 前提**で、
複数行にすると作り直しになる。とくにサイト判定バッジは「この URL がどのサイトか」を
1 件に対して出すものなので、複数行の入力欄とは噛み合わない。
1 本だけ落とす大多数のユーザーに複数行の入力欄を見せるのも、v1 の「徹底ミニマル」に反する。

**代わりに「入力欄は 1 行、URL はキューに積む」構成にする。**
複数まとめて投入したいときは、**改行・空白区切りの複数 URL を貼れば全部積まれる**
（ブラウザや X からのコピーは複数行になることが多く、これで「まとめて 5 本」は 1 操作で済む）。

### 7.3 画面（1 件のときは v1 と完全に同じ見た目）

**キュー一覧は 2 件以上あるときだけ現れる。** 1 件だけのときは v1.1/v1.2 と
1 ピクセルも変わらない。これは回帰を避けるためでもあり、
「1 本落とすだけの人に余計なものを見せない」という v1 方針の維持でもある。

```
  小説のURLを貼り付け                          [日本語|English]
  [ https://…                         ] [📋] [＋]
  ✓ 小説家になろう
             【 ⏸ 中止 】
  [2/5] 作品B  ―  第 12 話 / 全 30 話  （残り約 3 分）
  ████████████░░░░░░░░
  ── ダウンロード一覧  2 / 5 件 ──────────── [ ✕ 消す ]
   ✅ 作品A            小説家になろう
   ⏬ 作品B            カクヨム          第 12 話 / 全 30 話
   ⏳ 作品C            小説家になろう
   ⏳ 作品D            ステキブンゲイ
   ⚠ https://example… ―                未対応のサイト
  ──────────────────────────────────────────
  保存先： ダウンロード\小説
  ▸ 詳細設定
```

- `＋` ボタン＝**積むだけ**。`⬇ ダウンロード`＝**積んで開始**
- **実行中は大ボタンが「中止」になるので、`＋` が唯一の追加手段**になる。だから `＋` は必要
- 一覧は `CTkScrollableFrame`。件数が増えても窓が伸び続けないようにする

### 7.4 ジョブの構造 — ここが §8 への布石

```python
{
  "kind":      "download",     # v1.3 は download のみ。§8 で "append" が増える
  "target":    url,            # kind="append" のときは .txt のパス
  "label":     表示名,          # 初期は URL、サイト判定後はサイト名、DL 後は作品名
  "site_name": "小説家になろう",
  "info":      {...},          # --detect-site の結果（実行時に使い回す）
  "status":    "waiting" | "running" | "done" | "error" | "skipped",
  "detail":    "エラー: …",     # 失敗理由（_error_detail の結果）
  "epub":      "…/作品.epub",
  "n": 0, "total": 0,          # 話数進捗
}
```

**`kind` で `_build_cli_args` が分岐する**のが肝。これがあるおかげで §8 の
「続きを取得」（`--append FILE`）が**キューにそのまま乗る**。
v1.3 の時点では `"download"` しか作らないが、分岐点だけ先に用意しておく。

### 7.5 終端処理を 1 箇所に集約する（最重要の改修）

現在、ダウンロードの終端は **3 経路に散っている**（`novel_downloader_gui.py` `_handle_msg`）:

| 経路 | 発生元 | 現在の処理 |
|---|---|---|
| `precheck` | 未対応サイト判定 | `_set_state_error("unsupported")` |
| `aborted` | 起動前の中止 | `_set_state_idle()` ＋「中止しました」 |
| `finished` | エンジンの終了コード | rc で done / error を分岐 |

キューを足すとき、この 3 箇所それぞれに「次のジョブへ進む」を書くと必ず取りこぼす。
**`_job_finished(status, detail)` という単一の出口を作り、3 経路とも必ずそこを通す。**

```
precheck ─┐
aborted  ─┼─→ _job_finished(status, detail)
finished ─┘        │
                   ├─ 次に waiting があり、中止されていない → _start_job(次)
                   └─ 無い → _queue_done()（サマリー表示）
```

**1 件だけのキューを流したとき、v1.2 と同じ画面遷移になること**を
既存の回帰テスト 51 項目で担保する。

### 7.6 中止の意味は「キュー全体を止める」

| 案 | 判断 |
|---|---|
| 中止＝キュー全体を止める | **採用** |
| 中止＝現在の 1 件だけ飛ばして次へ | 不採用 |

「5 本積んだが、やっぱりやめる」が普通の期待で、1 件ずつ中止させると 5 回押す羽目になる。
止めたあと残りは `waiting` のまま**残す**ので、`⬇ 再開` で続きから流せる。
「この 1 件だけ飛ばす」は一覧の各行から消せばよく、専用ボタンは足さない。

### 7.7 1 件失敗しても止めない

- 失敗したジョブは `status="error"` にして**次へ進む**。5 本中 3 本目が消えていても残り 2 本は取れる
- 未対応サイトは実行前に `skipped` に倒す（エンジンを起動しない）
- 全件終了後にサマリー：`5 件中 4 件完了 / 1 件失敗`
- **失敗分だけ再試行する**ボタンを出す（`error` のジョブを `waiting` へ戻して再開）

### 7.8 重複の扱い

同じ URL（`normalized_url` で比較。無ければ生 URL）が既にキューにあれば積まず、
バッジに「すでに一覧にあります」と出す。出先で同じ作品を 2 回投げるのは
§8 の受信箱で普通に起きるので、ここで吸収しておく。

### 7.9 サイト判定は 1 本の専用スレッドで直列化する

`--detect-site` は**オフライン・即時**（`CLAUDE.md`）だが、exe の起動コストはある。
積むたびにスレッドを立てると 5 本で 5 プロセスが同時に上がる。

- **判定専用スレッド 1 本＋ `queue.Queue`** で、積まれた順に 1 件ずつ判定する
- 判定が済むまで一覧の行は「確認中…」。未対応と分かった時点で `skipped` に倒す
- ダウンロード実行中でも判定は止めない（オフラインなのでエンジンと競合しない）
- v1.1 の入力欄バッジ（`_detect_seq` の世代管理）とは**別経路**。混ぜない

### 7.10 設定はキュー開始時に 1 回だけ固める

現在は `_start_download` がジョブごとに `_collect_settings()` を呼んでいる。
キューでは**開始時に 1 回だけ固め、走っている間の詳細設定の変更は反映しない。**
5 本の途中で保存先を変えたとき、前半と後半で出力先が変わるほうが事故になる。

### 7.11 進捗表示

- ステータス行：`[2/5] 作品B ― 第 12 話 / 全 30 話 （残り約 3 分）`
- 進捗バーは**現在の作品の話数進捗**。全体を 1 本のバーにしない
  （作品ごとに話数が桁違いなので、件数で割った全体 % は実態と合わない）

### 7.12 キューは保存しない（ただし閉じる前に確認する）

v1.3 では終了時にキューを保存しない。再開位置・設定の再現・出力先の存在確認など、
永続化は作り込みが跳ねる割に、**§8 の受信箱を入れればファイルとして残る**ので重複する。
代わりに、**未完了のジョブがある状態で閉じようとしたら確認ダイアログを出す。**

### 7.13 実装の段取り（この順に）

| Step | 内容 | 回帰の危険 |
|---|---|---|
| **1** ✅ | ジョブモデルと `_job_finished` への集約。**UI は出さない**。単一 URL を「1 件のキュー」として流し、v1.2 と同じ挙動になることを回帰項目で確認 | **最大**。ここだけ切って先に通す |
| **2** ✅ | キュー一覧 UI（`CTkScrollableFrame`）。2 件以上で出現 | 中 |
| **3** ✅ | `＋` ボタン、複数 URL の一括投入、重複判定、判定スレッド | 小 |
| **4** ✅ | サマリー、失敗分の再試行、終了時の確認 | 小 |

**Step 2 と 3 は 1 つにまとめて実装した。** Step 2 だけでは一覧に 2 件以上積む手段が
無く（＋ ボタンも一括投入も Step 3）、UI が出る状態を作れないため、分けても
動かして確かめられない。あわせて Step 4 のサマリーも入れた（複数件を流せるように
なった時点で、最後の 1 件の状態しか出ないのは誤解を招くため）。

**Step 1 を単独でコミットできる形にする。** ここが唯一リリース済みの挙動を壊しうる。

### 7.14 追加する UI 文言（ja / en 両方必須）

`queue_title`（ダウンロード一覧）/ `queue_count`（{n} / {m} 件）/ `queue_clear`（✕ 消す）/
`q_waiting`（待機中）/ `q_running`（取得中）/ `q_done`（完了）/ `q_error`（失敗）/
`q_skipped`（未対応のサイト）/ `q_checking`（確認中…）/ `queue_dup`（すでに一覧にあります）/
`queue_summary`（{total} 件中 {ok} 件完了 / {ng} 件失敗）/ `retry_failed`（失敗した {n} 件を再試行）/
`resume`（⬇ 再開）/ `add`（＋）/ `tip_add`（一覧に追加する）/ `confirm_close`（ダウンロードが残っています。終了しますか？）

### 7.15 確認項目

```
 1. URL 1 本だけの動作が v1.2 と完全に同じ（回帰 51 項目が通る）
 2. 1 件のときキュー一覧が出ない
 3. 複数行の URL を貼ると全部積まれる。対応外の行は skipped になる
 4. 実行中に ＋ で追加でき、現在の DL が終わると続けて処理される
 5. 3 本中 2 本目が失敗しても 3 本目が実行される
 6. 中止でキュー全体が止まり、残りは待機のまま残る。再開で続きから流れる
 7. 同じ URL は二重に積まれない
 8. 全件終了後にサマリーが出る。失敗分だけ再試行できる
 9. キュー実行中に保存先を変えても、走っているキューの出力先は変わらない
10. 未完了のまま閉じようとすると確認が出る
11. 一覧が 20 件でも窓が伸び続けない（スクロール）
12. 英語 UI に日本語が混ざらない
13. DPI 倍率 1.5 で一覧を出しても §6.1 の増殖が起きない
```

### 7.15b Step 1 実装の記録

- `_make_job` / `_queue_start` / `_advance` / `_start_job` / `_job_finished` / `_queue_done` を追加
- 終端 3 経路（`precheck` / `aborted` / `finished`）を `_job_finished` に集約
- `_build_cli_args(job, s)` へ署名変更（§8 の `kind="append"` 用の拡張点）
- `_download_worker(job, s)` へ署名変更。`job["info"]` を使い回す
- 設定はキュー開始時に 1 回だけ固める（`_queue_settings`）

**実装中に見つかった不具合（修正済み）**:
起動時に始まったクリップボード判定の結果が、**ダウンロード中に届いて URL 欄を
書き換える**ことがあった。`_maybe_autofill_from_clipboard` は開始前に `_proc` を
見ているが、結果を受け取る `autofill` ハンドラ側が見ていなかった。判定は数秒かかるため、
その間にダウンロードが始まると、実行中の URL 欄が別の作品を指してしまう。
ハンドラでも `_proc` を再確認するようにした。

**テストについての教訓**:
`settings` 辞書を直接書き換えるテストは、`_queue_start()` の `_persist()` が
ウィジェット値から設定を組み直すため**黙って上書きされる**。設定を変えるテストは
必ずウィジェット変数（`var_auto_paste` など）を操作すること。
この取り違えで 2 度、テストが「通っているのに何も検証していない」状態になった。

### 7.15c Step 2/3 実装の記録

**一覧の出現条件**（§7.3 の「2 件以上」を実装時に詰めたもの）:

```python
len(self._jobs) >= 2 or any(j["status"] == "waiting" for j in self._jobs)
```

`＋` で 1 件だけ積んだ状態も出す。出さないと、入力欄が空になった分だけ見えて
**積んだ URL が消えたように見える**。単発ダウンロード中は 1 件が `running` で
待機が無いので隠れ、v1 と同じ見た目になる。

**バッチの畳み込み**: 新しく積むとき、前のバッチが完全に終わっていれば
`_jobs` を空にしてから積む。これが無いと 1 本ずつ落とすたびに済んだ行が溜まり、
一覧が出っぱなしになって「1 件なら v1 と同じ」が崩れる。

**同一性で探す**: `_job_index` は `is` で比較する。`list.index()` や `in` は
`==` なので、内容が同じ別ジョブと取り違える。

**実装中に直した不整合**: `📋` でまとめて投入したとき、`＋`（`_add_from_entry`）と
違って入力欄が消えず、前の URL が残っていた。一覧に移したものが入力欄にも
残ると「どちらが実行対象か」が分からないので、挙動を揃えた。

### 7.15d 実機で見つかった不具合（Step 2/3 の後）

**(1) 入力欄の判定を使い回した行だけサイト名が出ない**

一覧の 1 行だけ `[ネオページ]` が付かない状態になった。
`_add_urls` は入力欄のバッジで判定済みの URL について判定スレッドを省く
（`--detect-site` の exe 起動 1 回分の節約）が、`_make_job` が
`"site_name": ""` を固定で入れていたため、**判定スレッドを通らない経路では
誰も `site_name` を埋めなかった**。`_make_job` が `info["display_name"]` から
埋めるようにした。

> 最適化（判定の使い回し）が、**その副作用として行われていた初期化まで
> 一緒に飛ばしていた**という形。分岐で処理を省くときは「省いた側で
> 何が行われなくなるか」を数え上げること。

**(2) 大ボタンの `⬇` が豆腐になる**

Windows 実機で「⬇ ダウンロード」の矢印が □ で表示された。
一覧の `⏳` や補助ボタンの `📂` は描画できているので、フォントが無いのではなく
**大ボタンだけ `CTkFont(size=16, weight="bold")` で、太字だと Tk の
フォールバックが絵文字フォントに落ちない**と見られる。

- 大ボタン（`download` / `cancel` / `retry` / `resume` / `paste_dl`）の記号を
  基本フォントにある `↓`(U+2193) と `■`(U+25A0) に替えた
- 通常ウェイトの一覧記号・補助ボタンはそのまま（実機で描画を確認済み）
- **規約: 太字・大サイズのウィジェットに絵文字ブロックの文字を置かない。**

### 7.15e コードレビューで出た指摘（6 件・全て修正済み）

リリース前に `/code-review` を実施。5 件の指摘を受け、実機スクリーンショットから
自分で見つけた 1 件と合わせて 6 件を直した。**いずれも「複数件を流せるように
なったことで初めて成立する状態」を取りこぼしたもの**で、単発ダウンロードでは
起きない。

| # | 症状 | 原因 |
|---|---|---|
| R1 | 中止したのに緑で「3 件すべて完了しました」 | `_queue_done_summary` が `ng` を `error`/`skipped` だけで数え、**`aborted` と残りの `waiting` をどちらにも数えていなかった**。§7.6 で中止後に残りを `waiting` のまま残す仕様にしたのに、集計側を合わせていない |
| R2 | 一覧を出したまま閉じると、次回の窓が毎回 170px 高くなる | `_on_close` が `DETAIL_DELTA_PX` / `LOG_DELTA_PX` は引いていたのに `QUEUE_DELTA_PX` を引いていない。**detail/log と違い、これはセッションをまたいで累積する** |
| R3 | ＋ で積んだ 1 件が未対応だと、一覧ごと消えて行方不明 | `_queue_should_show()` が「2 件以上 or 待機中あり」。`waiting → skipped` に変わった瞬間に条件が偽になり一覧が閉じる。入力欄は既に空なので**その URL が画面から完全に消える** |
| R4 | 「再開」がクリップボードの無関係な URL を足して落とす | `_on_main_button` がクリップボードを読んでから待機中を見ていた。ボタンは「再開」と名乗っているのに別の URL を足す |
| R5 | 「失敗した N 件を再試行」が無反応のボタンとして残る | 新バッチを積むと `_add_urls` が `_jobs` を作り直して `error` を捨てるのに、ボタンを消していない |
| M1 | 新バッチを積んでも前バッチの完了表示・補助ボタンが残る（実機スクショで発見） | 同上。**「ログを保存」が前バッチのログを出す**ので見た目だけの問題ではない |

**修正の共通形**: `_reset_result_view()` を作り、バッチを畳むときに
ステータス行・補助ボタン・進捗バー・`_epub_path` をまとめて消す（R5 / M1）。
`_queue_staged` フラグで「ユーザーが明示的に積んだバッチか」を持ち、
一覧の出現条件を `len >= 2 or (staged and jobs)` にした（R3）。

**あわせて直した非対称性**: `_resize_height` が画面上限でクランプされると
戻りきらなかった（実測 2030 → +170 で 2080 に頭打ち → -170 で 1910）。
切り詰められた分を `_resize_debt` に覚えて、縮めるときに差し引く。

**テストについての教訓（3 度目）**: レビュー指摘の検証を既存スイートに
足したところ、5 件が「前のテストの後始末が効いていない」ために落ちた。
原因は `_on_close` が `_poll_queue` を止める**終端操作**であること。
1 つのアプリを使い回すスイートに終端操作を混ぜると、以降のテストが
沈黙して動かなくなる。**シナリオごとにまっさらなアプリを作る**
`review_fix_test.py` に分離した。

### 7.16 採らなかった案

| 案 | 判断 | 理由 |
|---|---|---|
| URL 欄を複数行テキストボックスに | **不採用** | §7.2。v1.1 の入力まわりが全部作り直しになり、サイト判定バッジが意味を失う |
| 並列ダウンロード（2〜3 本同時） | **不採用** | `--delay 1.5` は相手サイトへのマナーであって速度制限ではない。並列にすると実効間隔が 1/N になり、規約違反すれすれの挙動になる。速度が欲しい場面でもない |
| 全体進捗バー（件数ベース） | 不採用 | §7.11。作品ごとの話数が桁違いで実態と合わない |
| キューの永続化 | **保留** | §7.12。§8 の受信箱と重複する |
| ジョブごとに設定を変えられるようにする | 不採用 | 「この作品だけ横書き」は需要が薄く、一覧の各行に設定 UI を生やすことになる |

---

## 8. v2.0 設計（受信箱＋本棚）

§2 の入口2つを、**ダウンロード画面の下に折りたたみ節として**置く。上から下へ一方向に流れる。

```
┌────────────────────────────────────────────┐
│ 小説のURLを貼り付け              [日本語|EN] │
│ [_________________________]  [📋] [＋]      │
│            ⬇ ダウンロード                    │
│ ステータス / 進捗バー                        │
│ ダウンロード一覧（既存 §7）                  │
│ ▸ 📥 受信箱（2件）                           │
│ ▾ 📚 本棚（12件・新着3）    [🔄 新着チェック] │
│    ● 作品A なろう    312話 🆕+4 [続きを取得] │
│    ○ 作品B カクヨム   88話 最新              │
│ ▸ 詳細設定（保存先・表紙などの変更）         │
└────────────────────────────────────────────┘
```

### 8.0 【着手時の再調査】設計書の前提が 3 点崩れていた

§8 に着手する時点で現行コード・実エンジンと突き合わせた結果、旧稿の前提が
3 つ成立しないことが分かった。**以下はすべて実測**。

#### (1) 「新着チェックは `--check-update-dir --progress-json` で実装済み」→ **誤り**

このモードで stdout に出るイベントは `stage` だけで、**1 作品ごとの結果
（新着話数・タイトル・エラー）は日本語の人間向け出力にしか存在しない**。

```
$ novel_downloader.py --check-update-dir DIR --progress-json
{"schema":1,"event":"stage","n":1,...}      ← stdout はこれだけ
{"schema":1,"event":"stage","n":2,...}
--- stderr ---
[更新あり] 1 作品:
  水属性の魔法使い — 新着 932 話 （0003　氷の生成〜0886　涼の夢、叶う（後編））
```

GUI が結果を得るには `[更新あり]` 以降を正規表現で読むことになり、これは
§10 が `--dry-run` について戒め、`workinfo` イベントを足して解決したのと
**まったく同じ失敗の再発**にあたる。→ §8.1 (a) で解決する。

#### (2) 「本棚は `.txt` を走査するだけ・エンジン追加ゼロ」→ **見た目より重い**

ヘッダの `話数：` は**サイト側の総話数**であって手元のファイルの話数ではない。

```
$ grep -c "は大見出し］" X.txt   → 3     （実際に持っている話数）
$ grep "^話数：" X.txt           → 話数：935  （サイトの総話数）
```

手元の話数を出すには `_load_existing_txt()` 相当（区切り線 2 本の探索 →
`［＃改ページ］` 分割 → 末尾セクションからの奥付切り落とし）を GUI へ
**再実装**することになる。青空文庫書式が変わると黙ってずれる。
→ §8.1 (b) で解決する。

#### (3) 「`CTkTabview` へ」→ §6.1b / §6.1c と衝突する

`_fit_window()` / `_fit_flexible_panels()` は**ルートの `winfo_reqheight()`**
を測って詳細設定・一覧の見える高さを配分している。タブ化すると reqheight が
**全タブの最大**になるため、本棚タブの中身がダウンロードタブの高さ配分を
狂わせ、§6.1（DPI スケーリング下のウィンドウ増殖）を再発させかねない。
→ §8.5 のとおり**タブ化しない**。

### 8.1 エンジンに足す 2 つ（どちらも読み取り専用）

§10 の「エンジン側の新規実装はゼロ」は §8.0 (1)(2) により**改訂する**。
ただし追加は**読み取り専用・オフライン or 既存処理への相乗り**の 2 つだけで、
ダウンロード本体には一切触れない。**`--progress-json` 未指定時の挙動は不変**。

#### (a) `checkresult` イベント（`design_progress_json.md` §3.2）

```json
{"schema":1,"event":"checkresult","file":"作品A.txt","path":"/…/作品A.txt",
 "title":"水属性の魔法使い","author":"久宝　忠",
 "existing":3,"total":935,"new":932,"status":"updated","error":""}
```

- 送出点は `_check_update_one()` の**戻り値 1 箇所**。この関数は既に
  同じ内容の結果辞書を組み立てて返しており、**送出点を足すだけで
  `--check-update-dir` と `--append-dir` Phase 1 の両方が賄える**
- 単発 `--check-update FILE` は `_check_update_one()` を通らず
  `_main()` の `except _CheckUpdateDone` で処理されるので、そこにも同じ形で置く
- **`new_titles` は載せない。** 935 話の作品で 932 件が 1 行の JSON に載る。
  GUI は件数しか使わない。題名が要るときは生ログ（stderr）にある
- `status` は `updated` / `uptodate` / `error` の 3 値（既存の結果辞書のまま）

#### (b) `--shelf-scan DIR`（読み取り専用モード）

`--list-sites` / `--detect-site` と同じ扱い。**オフライン・即時・JSON を
stdout に 1 本出して `exit 0`**。ネットワークには触らない。

```json
[{"path":"/…/作品A.txt","file":"作品A.txt","title":"…","author":"…",
  "url":"https://ncode.syosetu.com/n0022gd/","site":"narou",
  "display_name":"小説家になろう","episodes":3,
  "epub":"/…/作品A.epub","mtime":1758600000.0,
  "meta":{"site":"小説家になろう","genre":"ファンタジー","serial_status":"連載中",
          "updated":"2026-09-11","episode_count":935,"…":"…"}}]
```

| フィールド | 出どころ |
|---|---|
| `title` / `author` | `.txt` の 1・2 行目（`aozora_header` の書式） |
| `url` | `_extract_url_from_txt()` |
| `site` / `display_name` | `detect_site()` ＋ `_SITE_DISPATCH`（オフライン） |
| `episodes` | `len(_load_existing_txt(path)[0])` ＝ **手元の話数** |
| `meta` | `_extract_meta_from_txt()`（`話数` はサイト総話数なので別枠） |
| `epub` | 同名の `.epub` / `.kepub.epub` があればそのパス、無ければ `""` |

- **既存ヘルパーを流用するだけで新しい解析は書かない。** これが目的
- `底本URL：` の無い `.txt`（ユーザーが置いた無関係なテキスト）は
  `url:""` で返し、**行としては出す**（GUI 側で「本棚に出さない」を決める）
- 壊れたファイル 1 本で全体を落とさない（1 件ずつ try で握る）

### 8.2 ディレクトリモードの `stage` は「作品ごと」に出る

`--check-update-dir` / `--append-dir` は作品ごとにランナーを呼ぶので、
`stage` が **N 作品 × 3 回**流れる。1 本のダウンロードの段階表示だと思って
進捗に使うと、作品が変わるたびに 3/3 → 1/3 へ巻き戻って見える。

**GUI はディレクトリモードでは `stage` を無視し、`checkresult` の到着数で
「N / 全M 件」を出す。** `progress` イベント（話数）も同様に作品ごと。

> なお `run_narou` が `stage` を出すようになったのは §5.7 の修正から。
> それ以前も他 16 サイトは出していたので、この性質は新しく生じたものではない。

### 8.3 受信箱（入口B・`watcher.bat` の後継）

**フォルダ規約は `watcher.bat` と揃える。既存ユーザーがフォルダをそのまま使えること。**

| 項目 | 仕様 |
|---|---|
| 監視フォルダ | 設定で指定（既定なし）。`OneDrive\triger` / `G:\マイドライブ\triger` を想定 |
| 取り込み契機 | 起動時＋手動「🔄 取り込む」。定期スキャンは v2.1 |
| URL 抽出 | ファイル本文から `https?://\S+` を抽出。**`watcher.bat` は最初の1件のみだが、GUI は全件拾う**（メモに溜めて一括投入できる）。既存の `_extract_urls()` をそのまま使う |
| サニタイズ | **不要。** `watcher.bat` の `-replace '["%''<>|...]'` はバッチ経由でシェルに渡すための処置。Python は `subprocess` にリストで渡すのでシェルを介さない |
| 文字コード | UTF-8（BOM 可）→ 失敗したら cp932 でリトライ。スマホ由来は UTF-8 が通常 |
| 情報の先読み | 各 URL に `--dry-run --progress-json` を1回。`workinfo` で**タイトル・著者・全話数**が返る（§10・実測確認済み） |
| 重複判定 | 本棚（`--shelf-scan` の `url`）と突き合わせ。一致したら「取得済み」にし、取得ボタンを**「更新を確認」に化けさせる** |
| 処理済み | ダウンロード成功後に `done\` サブフォルダへ移動（**`watcher.bat` と同じ規約**） |
| 移動失敗 | クラウド同期中はロックされうる。3回リトライし、駄目なら行に残して次回へ |
| URL 無しファイル | 削除も移動もせず、`⚠` を付けて残す（誤検知でユーザーのメモを消さない） |
| 自動取得 | 設定で ON にできる（`watcher.bat` 相当の挙動）。**既定は OFF**。§2.1 のとおり「見てから決める」のが本筋 |

**`--dry-run` を挟むのが `watcher.bat` との決定的な差。**
出先で投げた URL は「何の作品か覚えていない」ので、タイトルと話数を見てから決めたい。
`--dry-run` は 1 リクエストで済むため、件数分叩いても負荷は小さい（1秒間隔）。

**取得は §7 のキューに積む。** 受信箱は「ジョブの入口」であって独自の実行機構を
持たない。`_add_urls()` に `workinfo` 由来の `info` を添えて積むだけ。

### 8.4 本棚（入口A・B 共通の出口）

**専用のインデックスファイルを持たない。** 出力先フォルダの `.txt` が実体で、
`底本URL：` 行が実質のインデックス。読み出しは `--shelf-scan`（§8.1 b）で行う。

**「エンジンが対応している」と「GUI から操作できる」は別**なので列を分ける。
旧稿はこの表の右端を「実装済み」の一語にしていたため、エンジンの実装状況を
GUI の機能一覧だと読める書き方になっていた（§8.13 の突き合わせで判明）。

| 操作 | エンジン呼び出し | エンジン | GUI |
|---|---|---|---|
| 一覧 | `--shelf-scan DIR` | §8.1 (b) で追加 | ✅ |
| 新着チェック | `--check-update-dir DIR --progress-json` ＋ `checkresult` | §8.1 (a) で追加 | ✅ |
| 続きを取得 | `--append FILE --progress-json` | 対応済み | ✅ |
| まとめて取得 | （`--append-dir` は使わない・下記） | 対応済み | ✅ |
| 再ePub化（表紙・フォント変更） | `--from-file FILE` | 対応済み | ❌ 未実装 |
| ePub→テキスト | `--from-epub FILE` | 対応済み | ❌ 未実装 |
| エピソード一覧 | `--list-only` | 対応済み | ❌ 未実装 |

**「まとめて取得」に `--append-dir` は使っていない。** 個別の `--append` ジョブを
§7 のキューへ積む形で実装した。作品ごとに進捗が見え、途中で止められ、
1 件失敗しても残りが続く（§7.7）——`--append-dir` を 1 プロセスで回すより
GUI の見せ方に合う。エンジン側の `--append-dir` は CLI / cron 用として残る。

- 行の表示は `● / ○`（新着の有無）＋ 題名 ＋ サイト名 ＋ **手元の話数** ＋
  `🆕 +N`（`checkresult` の `new`）＋ ボタン
- **新着チェックを走らせるまで `🆕` は出さない**（ヘッダの `更新日` は
  ダウンロード時点の値なので、それだけでは新着の有無は分からない）
- 「続きを取得」は `kind="append"` のジョブとして §7 のキューに積む。
  `_make_job` / `_build_cli_args` は**最初からこの分岐を想定して書いてある**
- 一覧は `--shelf-scan` の結果をそのまま保持する。`--append` が終わった行は
  その場で新着表示だけ落とし、**走査のやり直しはキューを流し終えてから 1 回だけ**
  行う（`_shelf_dirty` → `_shelf_flush_pending`）。
  **`_job_finished()` の中で `shelf_scan()` を呼んではいけない** —
  あそこは UI スレッドで、走査は本棚ぶんの `.txt` を全部読む（1 作品 4MB 級）。
  同期で呼ぶと 1 件追記するたびに画面が固まり、まとめ取得なら件数ぶん繰り返される

### 8.5 画面構成 — **タブ化はしない**

旧稿の `CTkTabview` 案は §8.0 (3) の理由で不採用。
受信箱・本棚は**詳細設定と同じ「▸ 見出しを押すと開く」折りたたみ節**として
`ROW_*` に追加する。

- 追加する行: `ROW_INBOX_BTN` / `ROW_INBOX` / `ROW_SHELF_BTN` / `ROW_SHELF`
  （`ROW_AUX` と `ROW_OUTDIR` の間）
- `_fit_flexible_panels()` の配分対象に受信箱・本棚を**足すだけ**で済む。
  ただし現在は「詳細設定・一覧」の 2 枠決め打ちなので、
  **N 枠へ一般化する**（`(open?, want, min, floor)` のリストを配分する形）
- 既定はどちらも閉じた状態。**v1 の「URL 欄＋大ボタンで完結」は一切変わらない**
- 開いている節は設定に記憶する（`inbox_open` / `shelf_open`）

### 8.6 実装の段取り（この順に）

1. **エンジン**: `checkresult` イベント（§8.1 a）
2. **エンジン**: `--shelf-scan DIR`（§8.1 b）
3. **GUI**: `_fit_flexible_panels()` を N 枠へ一般化（§8.5）
4. **GUI**: 本棚の節（一覧 → 新着チェック → 続きを取得）
5. **GUI**: 受信箱の節（取り込み → `--dry-run` 先読み → キューへ）
6. **GUI**: 設定キー追加（`inbox_dir` / `inbox_auto` / `inbox_open` / `shelf_open`）

**本棚を先にやる。** 受信箱の重複判定が本棚の索引に依存するため。

### 8.8 【実機で発覚】短縮URLを GUI が門前払いしていた

実機の受信箱（`temp_sample/testtriger20260923`）に **Google 共有リンク
（`https://share.google/…`）** を 4 本入れたところ、**全件が「未対応のサイト」で失敗**した。

**原因は GUI の事前チェック。**

| 層 | 挙動 |
|---|---|
| エンジン本体 | `share.google` は `_SHORT_URL_HOSTS` にあり `expand_short_url()` で展開できる。**落とせる** |
| `--detect-site` | **オフライン契約**なので展開しない。必ず `site: null` を返す |
| GUI | その `site: null` を「未対応のサイト」と断定し、**エンジンを起動すらしない** |

§3.4 でハーメルンについて直したのと同じ失敗（GUI が先回りして門前払い）を、
別の入口で繰り返していた。**入口Bは出先のスマホで共有するのが本筋**なので、
共有リンクが短縮URLなのは例外ではなく既定だった。

**確定仕様:**

1. **エンジン `--detect-site` に `"short_url": true|false` を足す。**
   `is_short_url()`（`_SHORT_URL_HOSTS` のホスト名照合のみ）なので**オフラインのまま**。
   「今は判定できないが展開すれば分かるかもしれない」ことだけを伝える
2. **エンジン `workinfo` に `"url"`（展開・正規化後の作品URL）を足す。**
   短縮URLを投げた呼び出し側はこれでしか正体を知れない。
   受信箱の重複判定（本棚との突き合わせ）に要る
3. **GUI は `site is None` だけで未対応と決めない。** 判定を `is_unsupported(info)`
   に一本化し、`site is None and not short_url` のときだけ未対応とする。
   適用先はバッジ・大ボタンの活殺・一覧の事前スキップ・ワーカーの事前チェックの 4 箇所
4. **受信箱は短縮URLも `--dry-run` に通す。** 返ってきた `workinfo["url"]` で
   重複判定をやり直し、サイト名を取り直し、**ジョブの対象は実URLにする**
   （短縮URLのままだと毎回リダイレクトを追い直すうえ、一覧に正体が出ない）
5. バッジは赤（未対応）ではなく**橙**で「短縮URL — 開いてから判定します」

実測（4 本とも題名・話数まで取得できた）:

```
share.google/lFyUabU2K92vkODcx → kakuyomu.jp/works/2912051605198841131   55話
share.google/C72sSRMqWbcGcAGRw → kakuyomu.jp/works/2912051607778505262   82話
share.google/vb4Chz4FZTONpeSSW → kakuyomu.jp/works/11054822662461765393   8話
share.google/sBS0Z3aAj9bek3j7y → kakuyomu.jp/works/11054822662312485414  34話
```

#### 併せて直したもの: 受信箱がバイナリを読んでいた

同じフォルダにスクリーンショット（`.png`）が入っていた。`read_text_any()` は
UTF-8 → cp932 の順で読むが、**cp932 は多くのバイト列を黙って復号する**ため、
画像の中身を本文として URL 走査していた。

- 1MiB を超えるファイルは読まない（受信箱のファイルは「URL を書いたメモ」）
- 復号結果に NUL を含むならテキストでないとみなして捨てる
- **削除も移動もしない。** `⚠ URL が見つかりません` の行として残す（§8.3 のとおり）

---

### 8.9 【実機で発覚】詳細ログが詳細設定の中にあるように見える

ダウンロード後に「詳細を表示」を押すと、ログ欄が**詳細設定パネルより下**に出ていた
（`ROW_LOG` が `ROW_DETAIL` の後ろだった）。詳細設定が閉じていても、押したボタンは
画面上部の補助ボタン行、開いたものは画面最下部、という離れ方になり、
**何を開いたのか分からない**。開いていれば詳細設定の中身の続きに見える。

**確定仕様:**

- **詳細ログを受信箱・本棚・詳細設定と同じ折りたたみ節にする。**
  見出しは `_section_button()` で作り、意匠を 4 つとも揃える
- 位置は**補助ボタン行の直下**（`ROW_AUX` → `ROW_LOG_BTN` → `ROW_LOG` → `ROW_GRIP`
  → 受信箱 → 本棚 → 保存先 → 詳細設定）。結果まわりの情報なので結果の近くに置く
- **補助ボタンの「詳細を表示」は廃止し、節見出しに一本化する。**
  同じものを開く操作が 2 つあると、折りたたみ節にした意味が薄れる
- 見出しは**ログがあるときだけ出す**（`_sync_log_section()`）。
  実行中は `_start_job()` が `_raw_log` を空にするので自動的に消える。
  消えるときに開きっぱなしにしない（本体だけ宙に浮く）

ログ欄の高さは従来どおり `grid_rowconfigure(ROW_LOG, weight=1)` ＋ グリップで、
`_flex_panels()` の配分対象には**入れない**（窓の余りを吸う別の仕組みのまま）。

---

### 8.10 【実機で発覚】自動取得はあるのに、検知する仕組みが無かった

「受信箱に入った作品を自動で取得する」を ON にして GUI を立ち上げたままにしても、
**スマホからトリガーフォルダへ置いたことを検知する手段が無かった。**
結局「🔄 取り込む」を押す必要があり、自動取得の意味がほとんど無い状態だった。

原因は分け方の誤り。定期スキャンを §9（v2.1）送りにしたまま、
**自動取得のチェックボックスだけ v2.0 に出してしまった**。
`inbox_auto` は `_inbox_scan_finished()` でしか参照されないため、
その時点での意味は「走査したときに、見つかったものを取得する」でしかなかった。
さらに §8.3 が「取り込み契機は起動時＋手動」と書いていたのに、
起動時スキャンは**受信箱の節を開いた状態で終了していたときだけ**走っていた。

**確定仕様（定期スキャンを v2.0 へ前倒しする）:**

| 契機 | 条件 |
|---|---|
| 起動時 | `inbox_dir` があれば**節の開閉に関係なく** 1 回（`_inbox_boot`） |
| 定期 | `inbox_scan_min` 分ごと（既定 5・0 で無効・詳細設定で変更可） |
| 復帰時 | ウィンドウが `<FocusIn>` を得たとき。**直近 30 秒以内なら見送る**（alt-tab のたびに走らせない） |
| 手動 | 「🔄 取り込む」（従来どおり） |

**OS のファイル監視 API は使わない。** 入口Bの本命は OneDrive / Google ドライブの
同期フォルダで、同期で現れたファイルはイベントが素直に飛んでこない。周期で
見に行くほうが確実。`watcher.bat` の 2 秒間隔は人が出先で投げる頻度に対して過剰。

**走査を見送る条件**: ダウンロード実行中・本棚の新着チェック中・走査中・保留中。
先読みの `--dry-run` が同じサイトへ重ねて当たるのを避けるためで、
見送っても次の周期で拾える。

#### 【重要】本棚の走査を待たずに重複判定してはいけない

`_inbox_reload()` は当初、本棚が未走査なら `_shelf_reload()` を呼んだ直後に
**その場で `known`（本棚の URL 集合）を作っていた**。本棚の結果は非同期で
後から届くので、`known` は**空のまま**になる。

これが起動時スキャンでは毎回起きる（起動直後は本棚も未走査）。結果、
**取得済みの作品を「未取得」と判定し、自動取得が丸ごと落とし直す**。
935 話の作品なら 935 話を取り直して既存の `.txt` を上書きすることになる。

→ 本棚が未走査なら `_inbox_pending` を立てて**受信箱の走査を保留**し、
`_apply_shelf_rows()` で本棚が届いてから `_inbox_start_worker()` を呼ぶ。

#### 自動取得は同じファイルを周期ごとに叩き続けない

取れない URL（削除済み・未対応）はダウンロードが失敗し、失敗したファイルは
`done\` へ移らない（§8.3 のとおり、次回もう一度出すため）。素直に書くと
**5 分おきに永久に再試行する**。自動で一度試したファイルは
`_inbox_tried` に控え、この起動中は自動取得の対象から外す
（手動の「取得」は従来どおり効く）。併せて、判定の結果**未対応と分かった行は
選択を外して押せなくする**（`inbox_ng` 表示）。

走査が終わると受信箱の見出し下に「最終確認 HH:MM」を出す。
監視が生きていることが画面から分かるようにするため。

---

### 8.11 【セキュリティレビュー】受信箱は「共有フォルダ」を前提に書く

リリース前のセキュリティレビューで 2 件。どちらも**受信箱が新しく持ち込んだ攻撃面**で、
根は同じ「**監視フォルダの中身も名前も、書ける第三者に決められる**」という点。
入口Bの本命は OneDrive / Google ドライブ / 社内共有なので、これは想定外ではなく前提。

#### (1) 短縮URLの展開が、無操作で内部アドレスを踏みに行かせる（SSRF）

§8.8 で「`site is null` でも `short_url` なら通す」ようにし、§8.10 で
「定期スキャンが `--dry-run` で先読みする」ようにした。この 2 つが噛み合うと：

- 先読みは **`inbox_auto` に依存しない**（走査すれば必ず走る）
- 走査は起動時・N 分ごと・フォーカス復帰で**自動的に起きる**
- エンジンは `expand_short_url()` を `detect_site()` **より前**に呼ぶ
- `_follow_one_redirect()` はリダイレクト先のホストを検査していなかった

つまり共有フォルダに `https://bit.ly/xxxx` と書いたメモを置くだけで、被害者の PC が
**クリック無しで**攻撃者の決めた先（`192.168.1.1` や `169.254.169.254` 等）へ
GET を出す。資格情報は付かないので実害は「内部への強制 GET ＋ 到達確認」止まりだが、
GUI 化する前には存在しなかった経路。

**対策**: `_is_public_http_url()` を追加し、短縮URL展開の**各ホップで**検査する。
- `http` / `https` 以外は追わない
- ホストの解決先が 1 つでもグローバルでなければ追わない
  （プライベート・ループバック・リンクローカル・CGNAT 等）
- **名前解決できないホストは通す。** 到達できないので実害が無い一方、DNS が
  一時的に落ちているだけの正当な URL を弾きたくない

**`urlopen` は既定でリダイレクトを自前で追ってしまう**ので、こちらの 5 ホップの
合間で検査するだけでは素通りする。`HTTPRedirectHandler` を継承した
`_NoInternalRedirect` を `build_opener()` に噛ませ、**1 ホップずつ**止める。

検査は**短縮URL展開だけ**に効かせる。各サイトのスクレイパーは `detect_site()` /
`_host_matches()` で既知ドメインに限定済みで、そちらまで広げると副作用のほうが大きい。

#### (2) `done` をシンボリックリンクに差し替えられる

`_inbox_move_done()` は `os.makedirs(dest_dir, exist_ok=True)` してから
`shutil.move()` していた。**`os.makedirs(exist_ok=True)` は `done` が
ディレクトリへのシンボリックリンクでも成功する**ため、共有フォルダに
`done -> ~/.ssh` を作られると、**中身も名前も第三者が決めたファイル**が
リンク先に置かれる（`authorized_keys` など）。
`_inbox_worker` 側も `os.path.isdir()` がリンクを辿るため、
symlink → ファイルは本文として読んでいた。

**対策**:
- `done` が `os.path.islink()` なら何もしない
- 移動先が `os.path.realpath()` で受信箱の外を指したら書かない
- 走査時に `os.path.islink()` のエントリを飛ばす

ファイル名そのものは元から安全だった（`os.listdir()` 由来で `os.path.basename()`
を通すため `..` も区切り文字も入らない。既存ファイルは上書きせず連番退避）。

> OneDrive / Google ドライブはシンボリックリンクを同期せず、Windows での作成にも
> 昇格が要るため成立条件は限られる。ただし SMB / NFS / ローカル共有では現実的で、
> 対策が 3 行で済むので入れた。

#### 問題なしと確認した経路

| 経路 | 判断 |
|---|---|
| 受信箱の本文 → subprocess の argv | `_extract_urls` は `https?://…` しか返さないので `-` 始まりにならず、フラグとして解釈されない。`Popen` は全てリスト形式で `shell=True` 無し |
| 受信箱のファイル名 → `shutil.move` の宛先 | `os.listdir` 由来＋`basename` で走査不能。既存ファイルは上書きしない |
| `--shelf-scan` の `path` → `--append` の argv | `Path.resolve()` の絶対パスなのでフラグに見えない |
| `read_text_any` | 1MiB 上限と NUL 検査。復号後は URL 正規表現と Tk ラベルにしか流れない（markup 解釈なし） |
| `--shelf-scan` の JSON 出力 | 自プロセスの子との間のパイプのみ。`json.dumps` は生の改行を出さないので行分割注入もできない |
| `checkresult` / `workinfo` | 整数と既存の文字列のみ。`path` は `os.path.abspath` |
| 新しい致命エラー処理 | 例外文言を stderr に出すだけ。トレースバックは環境変数の後ろ |

---

### 8.12 【コードレビュー】リリース前に出た 9 件

セキュリティとは別に正確性のレビューを通した。**9 件すべて実在を確認して修正済み。**
半分は「受信箱・本棚が既存のキュー機構に相乗りした結果、**元からある前提を
踏み外した**」もので、残りは失敗経路の取りこぼし。

#### (1) 本棚・受信箱の取得ボタンがエンジンを 2 本同時に起動する（最重要）

`_shelf_append` / `_inbox_fetch_selected` は無条件に `_queue_start()` を呼んでいた。
他の入口には必ずガードがある（`_on_main_button` は実行中ならクリックを「中止」と
解釈し、`_shelf_check_updates` は `self._proc is not None` で降りる）のに、
この 2 つだけ抜けていた。行のボタンも実行中に無効化していない。

結果、ダウンロード中に「続きを取得」を押すと `_queue_start()` が
`_abort_event.clear()` で**進行中の中止要求を握り潰し**、`_start_job()` が
`self._proc` と `self._job_i` を上書きして**エンジンが 2 本走る**。
以後は進捗が別のジョブに記録され、中止は後から起動した方しか殺せない。

→ `_start_or_enqueue()` に集約。**空いているときだけ流し始める。**
実行中に積んだものは、今のジョブが終われば `_advance()` が拾う。

#### (2) 本棚の走査失敗が「空の本棚」と区別できない

`shelf_scan()` は例外で `[]` を返していた。`_apply_shelf_rows` はそれでも
`_shelf_loaded = True` を立てるため、§8.10 で入れたはずの
「本棚を待ってから重複判定する」ガードが**空の `known` で満たされてしまう**。
`inbox_auto` が入っていれば、取得済みの作品を全部「未取得」と判定して
第1話から落とし直し、既存の `.txt` / `.epub` を上書きする
——§8.10 が「起きてはいけない」と書いた事故そのもの。

→ **失敗は `None`、空は `[]`** で区別する。`None` なら `_shelf_loaded` を
立てず、保留中の受信箱走査も畳んで画面に理由を出す。

#### (3) 重複判定が完全一致で、末尾スラッシュ・大文字で外れる

`normalize_url()` が書き換えるのは**話数URL**だけで、作品トップURLは素通し。
一方 `run_narou` は `.txt` に `ncode` を小文字＋末尾スラッシュ付きで書く。
`https://ncode.syosetu.com/N9623LP`（大文字・スラッシュ無し）を投げると
「未取得」に化け、自動取得が落とし直す。エンジンの `_find_txt_by_url` は
同じ理由で両側を `rstrip("/")` している。

→ `canon_url()`（`rstrip("/")` ＋小文字化）を通して比べる。対応 17 サイトの
作品 ID は数字・16進・なろうの ncode（大小区別なし）で、大小で別作品になる
ものが無いため、まるごと小文字化してよい。

#### (4) 一時的な未マウントで受信箱フォルダの設定が永久に消える

`load_settings()` が `inbox_dir` の実在を見て、無ければ空にしていた。
空にした値は `_collect_settings()` がそのまま保存するので、**最初の
`_persist()`（パネル開閉やチェックボックス 1 つ）でディスクに `""` が焼き付く。**
OneDrive やネットワーク共有がマウントされる前に起動しただけで設定が失われる。

→ **保存値は消さない。** 使えるかどうかは走査の直前に見る（元から見ている）。

#### (5) 通信エラーが「ファイルの読み書きに失敗しました」と表示される

`_friendly_error()` の最後の `OSError` フォールバックに 2 つ落ちていた。
(a) `requests` の例外判定を `_KAKUYOMU_AVAILABLE` で囲っていたが、
あのフラグは **requests と bs4 の両方**が要る条件で、monogatary.com は
requests だけで動く（CLAUDE.md の依存表）。bs4 未導入環境では通信エラーが
ファイルエラーに化ける。(b) `ConnectionResetError` /
`http.client.RemoteDisconnected` / `socket.timeout` は urllib が `URLError` に
包まないまま上がってくる。トレースバックを既定で隠した以上、
文言が間違っていると利用者を見当違いの方向へ向かわせる。

→ 例外判定を `_REQUESTS_EXC` に切り出し、ファイル用フォールバックの手前に
接続エラーの枝を足す。

#### (6) 本棚チェックのスレッドに `try/finally` が無い

`proc.stdout` の反復や `proc.wait()` で例外が出ると `_shelf_proc` が
`None` に戻らず、チェックボタンが無効のまま・**受信箱の定期スキャンも
永久に止まる**（`_inbox_maybe_scan` の見送り条件に引っかかるため）。

→ `try/finally` で必ず後始末する。併せて、このプロセスの stderr に
`_read_log` を使っていたのをやめた。あれは進捗行と ePub 完了行を拾って
**ダウンロード用のイベント**に変えるので、作品ごとに進捗行を出す
`--check-update-dir` に噛ませると本棚を見ているだけで進捗バーが動く。
`_read_log_plain`（生ログに流すだけ）を分けた。

#### (7)(8) 失敗経路の取りこぼし 2 件

- `_inbox_tried` を `_add_jobs` の**前**に付けていた。`_add_jobs` は同じ対象が
  既にキューにあると黙って捨てるので、**一度も落としていないファイルが
  二度と自動取得されなくなる**。`_add_jobs` が積めたジョブを返すようにし、
  その分だけ印を付ける
- `_inbox_worker` に `try/finally` が無く、例外で `("inboxdone",)` を出し損ねると
  `_inbox_scanning` が `True` のまま残って**受信箱が黙って死ぬ**

#### (9) symlink の `.txt` が永久に「未チェック」になる

`shelf_scan` は `Path.resolve()`（リンクを辿る）で行を作り、`_emit_checkresult` は
`os.path.abspath()`（辿らない）で `path` を出していた。GUI はこの `path` を鍵に
本棚の行を引くので、`.txt` 自体がシンボリックリンクだと鍵が一致せず
🆕 が出ず「続きを取得」も押せないままになる。

→ `_emit_checkresult` も `os.path.realpath()` に揃える。

#### 指摘されなかった（確認済み）

短編フォールバックのタプル形状、`_FATAL_ERRORS` が `AbortRequested` /
`_CheckUpdateDone` / `SystemExit` を飲み込まないこと、`--append` ジョブが
`--output-dir` を付けずに表紙・フォント設定を活かすこと、
`--shelf-scan` が `--progress-json` の stdout 差し替えから除外されていること。

---

### 8.13 CLI と GUI の突き合わせ（抜けの棚卸し）

v2.14.0 の後、**CLI 40 オプションに対して GUI が何を使えていないか**を機械的に
突き合わせた。「CLI 全機能を GUI に出す」のは §11 のとおり採らないが、
**出すつもりだったのに繋がっていないもの**を見つけるのが目的。

内訳: ダウンロード時に渡す 11 / GUI が内部で叩く 6 / 使っていない 23。
23 のうち **GUI の別機能で代替済みが 9**（`--resume` は本棚の「続きを取得」、
`--check-update` は「新着チェック」、`--watch` 一式は受信箱＋本棚が引き継ぎ、
`-o` は題名からの自動命名）、**純粋な抜けが 14**。

#### 今回入れた 3 件

**(1) `--lang` が GUI からエンジンへ伝わっていなかった**

`_engine_env()` は `PYTHONUNBUFFERED` / `PYTHONUTF8` / `PYTHONIOENCODING` しか
渡しておらず、GUI を English にしてもエンジンは日本語のままだった。
これが目立つのは **`_error_detail()` がエンジンの stderr から失敗理由を拾って
ステータス行に出す**（§3.3）ため。英語 UI に `エラー: 作品が見つかりません…` が
混ざる。PR #1 の英語化も、§8.11 で足したエラー文言の英訳も、
**この 1 行が無いせいで GUI からは一度も見えていなかった。**

→ `_engine_env()` に `NOVEL_DOWNLOADER_LANG` を入れる。`--lang` を引数で渡すのではなく
環境変数にしたのは、ダウンロード以外（`--detect-site` / `--shelf-scan` /
`--check-update-dir` / `--dry-run`）にもまとめて効かせるため。
`_run_capture` のようなモジュール関数は設定 dict に手が届かないので、
モジュール変数 `_ENGINE_LANG` を `set_engine_lang()` で更新し、
`_apply_ui_lang()` から呼ぶ。

**(2) Webhook 通知を GUI から設定できなかった**

エンジンは `--notify webhook` / `--webhook-url` / `--webhook-format` に前から
対応していて、CLAUDE.md にも「通常 URL ダウンロード・`--append` ・
`--check-update-dir` で有効」と書いてあるのに、**GUI 利用者だけが使えなかった**。
§9 でも「設定画面から使えるように」と書いたまま残っていた。

→ 詳細設定の「動作」にチェックボックス＋宛先 URL ＋形式（discord / slack）。
`_webhook_args()` にまとめ、通常ダウンロード・`--append`・本棚の新着チェックの
3 経路へ渡す。

- **宛先が空のまま `--notify webhook` を渡さない。** エンジンは `parser.error` で
  即死する。`_webhook_args()` は両方揃ったときだけ引数を返す
- **検査は `normalize_webhook()` に切り出し、`load_settings()` と
  `_collect_settings()` の両方で通す。** 読み込み時だけに置いたら、画面で
  入力した直後の値が検査を通らず「宛先が空なのにチェックだけ入った状態」が
  画面に残った（実装中に実測で踏んだ）
- Webhook URL は事実上の資格情報で、`settings.json` に平文で入り、
  子プロセスの argv にも載る。**CLI が元からそうである**ことを踏まえた
  現状維持の判断で、隠す・暗号化するといった対処はしていない

**(3) `--no-inline-images` が GUI から使えなかった**

挿絵の取得はサイト側の作りに依存するため、仕様変更で壊れることがある。
CLI にはその避難口があるのに、**逃げ道のない GUI 利用者の側に無かった**。
→ 詳細設定の「ここから下は普段は変更不要」にチェックボックスを置く。

#### 【実機で発覚】Webhook のチェックが入らず、URL も貼れない（§8.13a）

上の (2) を入れた直後、実機で**チェックボックスを押しても ON にならず、
宛先の URL も入力できない**という詰みが出た。

原因は「通知には宛先が要る」という制約を、**CLI 引数ではなく画面の状態に
適用した**こと。`normalize_webhook()` が「URL が空なら `notify_webhook` を
False に戻す」をやっていたため：

```
チェックを入れる → _persist() → normalize_webhook が URL 空を見て False へ戻す
  → 読み戻してチェックが即座に外れる
  → _sync_notify_enabled() が URL 欄を disabled にする
  → 宛先を貼れない → 永久に有効にできない
```

URL が無いと有効にできず、有効にしないと URL を入れられない循環だった。

**確定仕様:**

- `normalize_webhook()` は**書式を整えるだけ**にする（形式 enum の矯正と
  URL の `strip()`）。**入力中の値を消さない・チェックを勝手に戻さない**
- 「使えるか」の判定は `webhook_ready()`（チェック済み＋宛先が `http(s)`）に分け、
  **`_webhook_args()` が CLI 引数を組む瞬間にだけ効かせる**。
  エンジンを `parser.error` で落とさないという当初の目的はここで達成される
- チェックは宛先が無くても自由に入れられる。代わりに
  **「宛先の URL を入れると送られるようになります」の注意書き**を出し、
  有効に見えて送られていない状態を黙らせない

**テストがこの誤動作を仕様として固定していた。** `smoke_gaps.py` には
「宛先が空ならチェックが外れる」という項目があり、**実装のバグをそのまま
期待値に書いていた**ため緑のまま通っていた。実機の操作順
（チェック → URL 貼り付け）をなぞる形に書き直した。
**UI の状態遷移は「関数を呼んで戻り値を見る」ではなく
「ウィジェットを操作して画面がどうなるか」で書かないと、この種は捕まらない。**

#### 残した抜け（今回は入れない）

| CLI | 判断 |
|---|---|
| `--from-file` / `--from-epub`（＋`--title` / `--author`） | 本棚の行の操作として自然で、エンジンは完成済み。**次にやるならここ** |
| `--list-only` | 同上。`--start` / `--end` の範囲選択 UI（§11）と組にして検討する |
| `--cover-bg` | 色の指定は §8.6 の「表紙の地の色」の決まり方（`theme_color` 優先）と噛み合わせる設計が要る |
| `--start` / `--end` | 範囲選択 UI が前提。単体で数値欄を 2 つ足すのは §11 の「徹底ミニマルが死ぬ」に触れる |
| `--no-epub` / `--newline` / `-o` / `--title` / `--author` | 詳細設定の肥大に見合わない。CLI で足りる |

---

### 8.14 【実機で発覚】本棚の表示が更新されない

「ダウンロードが済んだら本棚の表示を更新できないか。もしくは手動で更新する
方法はあるのか」という指摘。調べると **どちらもできなかった**。

| 経路 | 当時の挙動 |
|---|---|
| 新規ダウンロード完了後 | 更新されない。`_shelf_dirty` を立てるのは `_shelf_after_append()` だけで、**`kind == "append"` に絞っていた** |
| 追記（続きを取得）後 | 更新される（§8.12 で入れた） |
| 節を閉じて開き直す | **更新されない。** `_toggle_shelf()` は `not self._shelf_loaded` のときしか走査せず、一度読むとフラグは立ったまま |
| 手動ボタン | **無い。** 「🔄 新着チェック」はサイトへ問い合わせる別の操作 |
| 出力先フォルダの変更 | 更新される（唯一の経路だった） |

要するに、**起動後に一度本棚を開いたら、以後その表示は固定**だった。
落とした作品が本棚に出てこないのは当然で、回避策も無かった。

**確定仕様:**

1. **成功したダウンロードは種別を問わず本棚をずらす。** `_job_finished()` で
   `status == "done"` なら `_shelf_dirty = True`（`kind` で絞らない）。
   実際の走査は従来どおり `_queue_done()` の `_shelf_flush_pending()` で
   **キュー全体が終わってから 1 回だけ**（§8.12 のとおり、1 件ごとに走査すると
   まとめ取得で件数ぶん繰り返して画面が固まる）
2. **開いたときに「変わっていたら」読み直す。** `_shelf_stale()` を新設し、
   `_shelf_dirty` に加えて**出力先フォルダの更新時刻**（`_shelf_dir_mtime`）を
   比べる。CLI で落とした・別の PC から同期された・手で消した、のいずれも拾える。
   ファイルの**中身**の書き換えは mtime が動かないが、それは追記後の
   `_shelf_dirty` が受け持つ
3. **「一覧を更新」ボタンを足す。** 「🔄 新着チェック」との違いが一目で分かるよう、
   **絵文字を重ねず文言で区別する**（一覧の更新は手元のフォルダを読むだけで
   オフライン、新着チェックはサイトへ問い合わせる）

> **テスト環境の注意**: 完了時のフォルダ自動オープン（`open_folder_on_done`）は
> WSL では `xdg-open` がブラウザを起こして戻ってこない。`_open_folder()` は
> `subprocess.run()` をタイムアウト無しで呼ぶため、GUI を自動テストするときは
> この設定を切る。Windows では `explorer` / `os.startfile` が即座に戻るので
> 実害は無い。

---

## 9. v2.1 設計（常駐・通知）

| 項目 | 内容 |
|---|---|
| トレイ常駐 | `pystray`。最小化でトレイへ格納 |
| ~~定期スキャン~~ | **v2.0 へ前倒し済み（§8.10）。** 自動取得のチェックボックスだけ先に出してしまい、検知する手段が無い状態になっていたため |
| 定期更新チェック | 本棚を N 時間ごと（既定 6 時間）。`--check-update-dir` |
| 通知 | Windows トースト。`--notify webhook` / `--webhook-url`（**エンジン実装済み**）を設定画面から使えるように |
| 自動起動 | スタートアップ登録のチェックボックス |

---

## 10. エンジン（`novel_downloader.py`）側に必要な追加

**v1.1 〜 v1.3 はゼロ。v2.0 で読み取り専用の追加が 2 つ**（`checkresult` イベント・`--shelf-scan`）。
旧稿は「v2.0 もゼロ」としていたが、着手時の実測で崩れた。経緯は §8.0、仕様は §8.1。
**どちらも読み取り専用で、ダウンロード本体と既定の標準出力には触れない。**

| 必要になりそうなもの | 判定 |
|---|---|
| 進捗・完了の取得 | `--progress-json` で**実装済み**（`design_progress_json.md`） |
| サイト判定・一覧 | `--detect-site` / `--list-sites` で**実装済み** |
| 作品情報の先読み | `--dry-run` で**実装済み**。ただし出力が人間向け print のみ |
| 更新チェック・追記 | 実行は `--check-update-dir` / `--append` / `--append-dir` で実装済み。ただし**結果の機械可読な受け取り口が無かった** → `checkresult` を追加（§8.1 a） |
| 本棚の一覧 | `.txt` の走査で足りると見ていたが、手元の話数を数えるには `_load_existing_txt()` の再実装が要る → `--shelf-scan` を追加（§8.1 b） |

**唯一の検討事項:** §8.1 の受信箱で `--dry-run` の結果（タイトル・著者・全話数）を
GUI がパースする必要がある。これは `design_progress_json.md` §1 が戒めた
**「人間向け表示文言をプロセス間の契約にする」の再発**にあたる。

→ **解決済み（v2.12.0+）。** `workinfo` イベントを追加した（`design_progress_json.md` §3.1）。

```json
{"schema":1,"event":"workinfo","title":"水属性の魔法使い","author":"久宝　忠","total":935,"unit":"episode"}
```

- 発火点は `_dry_run_exit()`。**全スクレイパーが「作品情報を取得し終えた直後」に必ず通る
  唯一の地点**なので、17 サイト分をここ 1 箇所で賄える
- `unit` で `total` の単位を明示する（話数 / ページ数 / 章数はサイトによって違う）。
  青空文庫は ZIP 1 本で確定しないため `total` を送らない
- **既定の挙動は変えていない。** 17 サイト全てで `--dry-run` の人間向け出力が
  変更前と 1 バイトも一致することを実測で確認済み
- GUI 側は受け取ってジョブに控える（`job["label"]` / `author` / `total_known` / `unit`）。
  §7.3 のキュー一覧と §8.1 の受信箱がこれを表示する

---

## 11. 採らなかった案とその理由

| 案 | 判断 | 理由 |
|---|---|---|
| `watcher.bat` をそのまま GUI へ移植（検知即DL） | **不採用** | §2.1。「知らない作品」を取る前に見たい、という用途と噛み合わない。自動DLは設定で選べれば足りる |
| 受信箱に専用のインデックス DB を持つ | 不採用 | `.txt` の `底本URL：` で足りる。二重管理は破綻する |
| 2 秒ポーリング（`watcher.bat` 準拠） | 不採用 | 人が出先で投げる頻度に対して過剰。5 分で十分 |
| クリップボード常時監視 | **保留** | 常駐すれば技術的には可能だが、他アプリでコピーした URL を勝手に拾うのは気持ちが悪い。§5.4（フォーカス時）で目的は達する |
| 詳細設定に CLI 全オプションを並べる | 不採用 | v1 の「徹底ミニマル」が死ぬ。`--start`/`--end` は §8.2 のエピソード一覧から範囲選択する形にすればオプション欄を増やさずに済む |
| GUI をエンジンと同一プロセスに統合 | 不採用 | `gui_v1_design.md` §3 ①A の「クラッシュ非波及」「中断の確実性」は現在も有効 |
| §8 を `CTkTabview` の 3 タブにする | **不採用**（旧稿から変更） | §8.0 (3)。`_fit_window()` はルートの `winfo_reqheight()` で高さを配分しており、タブ化すると reqheight が全タブの最大になって §6.1 のウィンドウ増殖を再発させる。折りたたみ節なら配分対象を足すだけで済む |
| 本棚の一覧を GUI 側で `.txt` パースして作る | 不採用 | §8.0 (2)。話数のカウントは区切り線・改ページ・奥付切り落としの合わせ技で、青空文庫書式が変わると黙ってずれる。`--shelf-scan` でエンジンに一本化する |
| `--check-update-dir` の日本語出力を GUI が正規表現で読む | 不採用 | §8.0 (1)。`design_progress_json.md` §1 が戒めた「表示文言をプロセス間の契約にする」そのもの |

---

## 12. TODO

### v1.1（着手対象）
- [x] §3.1 `load_settings` の schema 比較を `<=` にしてマイグレーション対応
- [x] §3.2 `_fallback_epub_path` に開始時刻ガードを入れる
- [x] §3.3 エラー要約（`❌`/`⚠` の最終行）を表示
- [x] §3.4 `needs_playwright` での事前拒否をやめ、事後エラーに移す
- [x] §5.1 `_attach_context_menu()` を実装し URL欄・保存先欄に適用
- [x] §5.2 `Ctrl+A` / `Enter` / `Esc` を bind
- [x] §5.3 `📋` / `📋⬇` ボタン
- [x] §5.4 `<FocusIn>` でのクリップボード再チェック（判定条件1〜6）
- [x] §5.5 サイト判定バッジ（デバウンス＋世代カウンタ）
- [x] §5.6 実行中の URL 欄を `readonly` へ
- [x] §5.7 `stage` イベント受信（`label` は使わず `n` を対訳）
- [x] §5.8 UI 文言を ja/en 両方追加
- [x] §5.9 `auto_paste` 設定キー
- [x] §5.10 の確認項目を WSLg 上の実画面で自動テスト（51 項目・全通過）
- [x] 実エンジンとの結合テスト（なろうを 3 話ダウンロードし進捗・ePub 検出・ログ保存まで）
- [x] 実機 Windows での確認（1回目）→ §6.1 / §6.2 を検出・修正済み
- [ ] **実機 Windows での再確認**（§6.1 の増殖が直ったか・グリップの操作感・右クリックメニューの配色）
- [x] `python -m py_compile novel_downloader_gui.py`

### v1.2（実装済み）
- [x] §3.3 エラー要約の表示
- [x] §6 `📄 ログを保存`（ヘッダに URL・エンジン版・日時を付ける）
- [x] §6 `📖 ePubを開く`
- [x] §6 完了時の自動オープンを `open_folder_on_done` で切れるように
- [x] §6 残り時間の目安（実測ペースから算出。delay からの計算にはしない）
- [x] §6 エンジン版数の表示（**GUI 側に版数を持たない**＝版数の単一ソースは
      `novel_downloader.py` の `__version__`。`--version` を叩いて名乗らせる）
- [x] §6 ウィンドウ位置・サイズの記憶。詳細設定の開閉を**相対リサイズ**に変更
- [x] §6.1 DPI スケーリング下でウィンドウが増殖する不具合を修正（倍率 1.5 で検証）
- [x] §6.2 詳細ログを窓の余白に追従させ、グリップでドラッグ変更できるように
- [x] §6.3 終了時に予約済み `after` を停止
- [x] §6.4 `_open_epub` に拡張子ゲート（セキュリティレビュー指摘・防御的措置）
- [x] §3.3 エラー要約の目印をエンジンの実記法に訂正（`❌` は存在しなかった）
- [x] `_maybe_autofill_from_clipboard` の `_last_clip` 記録位置を修正
      （欄を消した後に自動貼り付けされない不具合）
- [x] ログ保存でエラー文を消さないように（5 秒後に復元）
- [x] メイン画面のグリッド行を定数化（`ROW_*`）
- [x] §5.9 `open_folder_on_done` / `window_geometry` 設定キー

### v1.3（実装済み）
- [x] §7 複数URLキュー（Step 1〜4・§7.15b〜e の指摘反映まで）

### エンジン積み残し（v2.0 着手前に解消）
- [x] §5.7 `run_narou` が `stage` を出さない → `_emit_stage()` で表示を変えずに解決
- [x] §3.3 404 等でエンジンが未捕捉例外になる → `_friendly_error()` / `_exit_fatal()`
- [x] `kky_fetch` / `alp_fetch` / `est_fetch` の `RuntimeError` に最後の例外を付ける

### v2.0（実装済み・実機確認待ち）
- [x] §8.1 (a) `checkresult` イベント（`--check-update-dir` / `--append-dir` / 単発 `--check-update`）
- [x] §8.1 (b) `--shelf-scan DIR`
- [x] §8.5 `_fit_flexible_panels()` を N 枠へ一般化（`_allocate_heights` の 3 段配分）
- [x] §8.4 本棚の節（一覧・新着チェック・続きを取得・まとめて取得）
- [x] §8.3 受信箱の節（取り込み・`--dry-run` 先読み・重複判定・`done\` へ移動）
- [x] §8.6 設定キー（`inbox_dir` / `inbox_auto` / `inbox_open` / `shelf_open`）
- [x] 入れ子で開閉したときにウィンドウが戻りきらない不具合（§8.7）
- [x] WSLg 上の実画面テスト（本体 31 項目・append 経路 10 項目・全通過）
- [x] §8.8 短縮URL（`share.google`）を GUI が門前払いしていた不具合（実機で発覚）
- [x] §8.8 受信箱がスクリーンショット等のバイナリを本文として読んでいた
- [x] §8.9 詳細ログを折りたたみ節にし、補助ボタン行の直下へ移す（実機確認での指摘）
- [x] エンジン: なろうの短編（全1話）がダウンロードできない不具合（実機で発覚）
- [x] §8.10 受信箱の定期スキャン・起動時スキャン・復帰時スキャン（§9 から前倒し）
- [x] §8.10 本棚を待たずに重複判定して取得済み作品を落とし直す不具合
- [x] §8.11 短縮URL展開のリダイレクト先検査（SSRF・セキュリティレビュー指摘）
- [x] §8.11 `done` シンボリックリンク経由の書き込み（同上）
- [x] §8.12 コードレビュー指摘 9 件（二重起動・走査失敗の誤判定・URL 照合・設定消失ほか）
- [x] §8.13 `--lang` の伝達 / Webhook 設定 / `--no-inline-images` を GUI に追加
- [x] §8.4 の表を「エンジン」「GUI」の 2 列に訂正（実装状況を取り違える書き方だった）
- [ ] **実機 Windows での確認**（受信箱のクラウド同期フォルダ・DPI 150% でのパネル 4 枠）
- [ ] `forwindows/README_GUI.txt` の更新（受信箱の説明・`watcher.bat` からの移行手順）

### 8.7 【実装時の発見】入れ子で開閉すると窓が戻りきらない

`_fit_window(restore_to=…)` の `restore_to` は「そのパネルを**開く直前**の高さ」だが、
別のパネルが既に開いていれば**その分だけ嵩上げされた値**になる。
外側を先に閉じて内側を後から閉じると、最後に残るのが嵩上げ済みの値になり、
**全部閉じたのに窓だけ広いまま**になる（実測: 420 → 全部閉じても 585）。

詳細設定とログの 2 枠でも起きていたが、パネルが 4 枠になって目に見えるようになった。
**何も開いていない状態へ戻るときは `restore_to` を `self._base_height`
（＝何も開いていないときの高さ）で頭打ちにする。**
- [ ] §9 常駐・通知
- [ ] `forwindows/README_GUI.txt` の更新（受信箱の説明・`watcher.bat` からの移行手順）
- [ ] `watcher.bat` / `watcherG.bat` の扱いを決める（当面は併存。v2.1 で「GUI へ移行してください」と案内）
