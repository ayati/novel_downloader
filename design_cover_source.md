# 設計書：表紙画像ソースの改善（テーマカラー採用 ＋ og:image 品質ゲート）

- 対象: `novel_downloader.py`
- ステータス: **実装済み**（2026-07-28。A・C ともに完了。B は §7 のとおり見送り）
- 調査日: 2026-07-28。全17サイトの og:image を実取得して寸法・バイト数まで実測した
- 起点: 「なろうの `--use-site-cover` が失敗する」という報告。調査の結果、**エラーではなく og:image の中身が
  表紙でなくなった**ことが原因と判明した
- スコープ: **A（自動生成表紙のテーマカラー採用）** と **C（`--use-site-cover` の品質ゲート）** の2件。
  検討したが**見送った案 B**（ジャンル別の既定色）は §7 に理由を残す

---

## 1. 実測結果

### 1.1 なろうの og:image は SNS カードに変わっている

```
https://ncode.syosetu.com/n0022gd/
  → og:image = https://sbo.syosetu.com/n0022gd/twitter.png
  → HTTP 200 / 99,129 B / image/png / 1200×630
```

取得は成功する。`--use-site-cover` を通すと最後まで完走し `表紙画像: /tmp/tmpXXXX.png` と表示され、
ePub も生成される。**壊れているのは中身**で、キャッチコピーの文字となろうロゴを描画した横長の
シェア用カードが表紙ページに入る。ユーザから見た「失敗」はこれ。

**カクヨムも同じ構造**だった。`cdn-static.kakuyomu.jp/works/{id}/ogimage.png`（1200×630）は
キャッチコピーを**その作品の `baseColor` で描いた**カードで、やはり表紙ではない。

### 1.2 全17サイトの og:image 実測表

| サイト | og:image 実測 | URL の特徴 | 判定 |
|---|---|---|---|
| なろう | 1200×630 PNG | `sbo.syosetu.com/{n}/twitter.png` | ✗ SNSカード |
| カクヨム | 1200×630 PNG | `cdn-static.../ogimage.png` | ✗ SNSカード |
| ハーメルン | 1200×630 PNG | `img.syosetu.org/ogp_{id}` | ✗ SNSカード |
| ソリスピア | 1200×630 PNG | `solispia.com/title-image/{id}` | ✗ SNSカード |
| ネオページ | 1032×544 PNG | `/v1/share-image/book/{hash}.png` | ✗ SNSカード |
| エブリスタ | 600×314 JPEG | `img.estar.jp/public/user_upload/og/` | ✗ 横長 |
| ノベルアップ＋ | 1000×1000 JPEG | `/s3/**common**/sc-2023-10-11.jpg` | ✗ **サイト共通**の既定画像 |
| 青空文庫 | 300×300 PNG | `/images/**top_logo**_300x300.png` | ✗ サイトロゴ |
| アルファポリス | 500×711 PNG | `/img/books/**no_image**/novel/fantasy.png` | △ ジャンル別プレースホルダ |
| monogatary | 750×750 PNG | `monogatary.imagewave.pictures/{id}` | △ 正方形 |
| 野いちご | 750×1102 JPEG | `/img/issuedProduct/{id}-750.jpg` | ✓ 本物の表紙 |
| ステキブンゲイ | 830×1080 JPEG | `/_ub/novel/{uuid}/{uuid}` | ✓ 本物の表紙 |
| berry's cafe | 166×250 JPEG | `/img/member/{id}/{slug}**-thumb**.jpg` | ✓ だが極小 |
| ノベマ！ | 250×342 JPEG | `/img/book-cover/{id}**-thumb**.jpg` | ✓ だが小さい |
| NOVEL DAYS | 200×320 JPEG | `/work/**thumb_**{hash}.jpg` | ✓ だが小さい |
| 杉田玄白 | og:image なし | — | — |
| 結城浩翻訳の部屋 | og:image なし | — | — |

**17サイト中 8サイトが「表紙にならない画像」を返す。** 現行コードはこれを無条件に採用している。

### 1.3 高解像度版が取れるサイトがある（実測）

URL から `-thumb` を外すと **2倍解像度**が HTTP 200 で返る。

| サイト | thumb（現行） | thumb 除去後 | 結果 |
|---|---|---|---|
| ノベマ！ | `1640364-thumb.jpg` 250×342 / 8 KB | `1640364.jpg` **500×684** / 21 KB | ✓ 200 |
| berry's cafe | `szohylz7pz-thumb.jpg` 166×250 / 12 KB | `szohylz7pz.jpg` **500×750** / 67 KB | ✓ 200 |
| 野いちご | `12698-750.jpg` 750×1102 | `12698-1200.jpg` は 750×1026 の別物、`12698.jpg` は 404 | 現行が最大 |
| NOVEL DAYS | `thumb_{hash}.jpg` 200×320 | `{hash}.jpg` は 404 | 上位版なし |

### 1.4 per-work のテーマカラーを持つのはカクヨムだけ

全17サイトを `*color*` を含む JSON キーと `<meta name="theme-color">` で走査した。

- **カクヨム**: `__NEXT_DATA__` → `__APOLLO_STATE__` → `Work:{id}` に `"baseColor":"#42B8C1"`。
  作者が54色から選ぶイメージカラー。**per-work で唯一の実用値**
- ステキブンゲイ `#83d1ff` / ハーメルン `#b3a98c` は `theme-color` meta にあるが**サイト共通のブランド色**で
  作品ごとではない。使わない
- 残り14サイトは色情報を持たない

カクヨムの `adminCoverImageUrl` / `promotionalImageUrl` / `adminSquareImageUrl` も確認したが、
書籍化作品2件を含め**すべて `null`** だった（カクヨム公式レーベル作品専用のフィールドと思われる）。
**カクヨムに実表紙の入手経路は無い** ため、自動生成表紙の品質を上げるのが唯一の改善策になる。

---

## 2. 現行コード確認結果

### 2.1 `cover_bg` の決定は main() の1箇所、消費は build_epub の中

```
_main()  :10946-10947   --from-file 時         args.cover_bg is None → "#16234b"
_main()  :11006-11008   URL モード時           args.cover_bg is None → _SITE_DISPATCH[site][1]
   ↓
run_*()  ×17            build_epub(..., cover_bg=args.cover_bg, meta=info, ...)
run_from_file() :8849   cover_bg = args.cover_bg or "#16234b"   ← ここだけ or で潰している
   ↓
build_epub() :2616  →  make_cover_image() :2453  /  _make_cover_svg() :2383
```

**重要**: main() が既定色を代入してしまうため、`run_*()` に入った時点で
`args.cover_bg` は**常に非 None** になっている。つまり現状では
「ユーザが `--cover-bg` を明示した」のか「サイト既定色が入っただけ」なのかを**区別できない**。
A の実装ではこの区別が必要になる（§3.1）。

### 2.2 `theme_color` は取得済みだが完全に死んでいる

```
novel_downloader.py:3811-3813   _kky_meta_from_work()
    color = (work.get("baseColor") or "").strip()
    if re.fullmatch(r"#[0-9A-Fa-f]{6}", color):
        meta["theme_color"] = color
```

`grep -n theme_color` の結果は**この1箇所のみ**。`_META_FIELDS`（`:447`）にも入っていないため、
`.txt` ヘッダーにも OPF にも出ず、`info` dict の中で捨てられている。
**A は実質「配線するだけ」**で済む。

### 2.3 自動生成表紙の配色（コントラスト上の制約）

`make_cover_image()` `:2461-2496` の描画は次のとおり。

| 要素 | 色 |
|---|---|
| 背景グラデーション 上端 | `_darken_color(cover_bg, factor=0.6)` `:232` |
| 背景グラデーション 下端 | `cover_bg` そのもの |
| 外枠（二重線） | `(200,180,130)` / `(180,160,110)` 金 |
| タイトル文字 | `(255,248,215)` クリーム（影 `(0,0,0,100)`） |
| 著者名 | `(220,205,170)` |

キャンバスは `_COVER_W, _COVER_H = 800, 1200` `:2157`。

**クリーム色の文字を前提にした配色**なので、明るい `cover_bg` を入れると文字が読めなくなる。
実測したカクヨムの色には `#FF9D00`（V=1.00）`#FF8A94`（V=1.00）`#FF5D81`（V=1.00）
`#BAC23E`（V=0.76）`#42B8C1`（V=0.76）などが含まれ、**そのまま流し込むと破綻する**。
逆端に `#000000` もあり、こちらは暗くしすぎるとグラデーションが消えて真っ黒になる。
→ §3.3 の明度クランプが必須。

### 2.4 `_fetch_ogp_cover()` の現状と呼び出し箇所

`:9555` 定義。処理は「ページ HTML 取得 → og:image 正規表現2種で抽出 → 画像 GET →
Content-Type/拡張子判定 → 一時ファイル書き出し」。**画像の中身は一切検査していない。**

呼び出しは2箇所で、どちらも直前に `site` 変数が確定している。

| 箇所 | 文脈 | `site` の入手元 |
|---|---|---|
| `_main()` `:11011-11015` | 通常の URL ダウンロード | `:10943` `site = detect_site(args.url)` |
| `_append_one()` `:10088-10093` | `--append` / `--append-dir` / `--watch` の追記 | `:10058` `site = detect_site(url)` |

→ **`site` を引数で渡せる**。サイト別の処理分岐を入れるのに追加の判定は要らない。

### 2.5 内部ランナー4箇所の `cover_bg`

`_make_runner_args()` `:9935` 経由の呼び出しは4箇所。

| 行 | 用途 | 現在の指定 | A 実装後 |
|---|---|---|---|
| `:9995` | `_check_update_one` | `cover_bg=default_color` | `no_epub=True` なので**無関係**・据え置き |
| `:10247` | `_check_update_url` | `cover_bg=default_color` | 同上・据え置き |
| `:10078` | **`_append_one`（実 DL）** | `getattr(base_args,"cover_bg",None) or default_color` | **`or default_color` を外す**（§3.1） |
| `:10476` | `run_watch` の新規 DL | `cover_bg=default_color` | **`getattr(args,"cover_bg",None)` に変更** |

---

## 3. 設計 A — 自動生成表紙にテーマカラーを使う

### 3.1 決定：色の解決を `build_epub()` の中に一本化する

素直に実装すると「17個の `run_*()` すべてで meta を見て cover_bg を差し替える」ことになるが、
**`build_epub()` は既に `cover_bg` と `meta` と `site_name` の3つを受け取っている**ので、
そこで解決すれば**変更点は4箇所で済み、17サイトのスクレイパーは一切触らなくてよい**。

**優先順位**

```
1. --cover-bg でユーザが明示した色       （args.cover_bg が非 None）
2. meta["theme_color"]                  （現状カクヨムのみ。将来他サイトが持てば自動で効く）
3. サイト既定色                          （_SITE_DISPATCH の表示名から逆引き）
4. "#16234b"                            （最終フォールバック）
```

**「明示したかどうか」の区別は、main() が既定色を代入するのをやめることで得る。**
`args.cover_bg` を None のまま `build_epub()` まで通し、既定色の適用を `build_epub()` に移す。
`build_epub()` は `site_name`（表示名 `"カクヨム"` 等）を受け取っているので、
`_SITE_DISPATCH` から**表示名 → 既定色**の逆引きマップを1つ作れば既定色を引ける。

```python
# _SITE_DISPATCH 定義（:9914）の直後に置く
_SITE_COLOR_BY_LABEL = {label: color for label, color, _fn in _SITE_DISPATCH.values()}


def _resolve_cover_bg(cover_bg: str | None, meta: dict | None, site_name: str) -> str:
    """表紙背景色を決める。優先順位は
    --cover-bg 明示 > meta["theme_color"] > サイト既定色 > "#16234b"。"""
    if cover_bg:
        return cover_bg
    tc = ((meta or {}).get("theme_color") or "").strip()
    if re.fullmatch(r"#[0-9A-Fa-f]{6}", tc):
        return tc
    return _SITE_COLOR_BY_LABEL.get(site_name, "#16234b")
```

`build_epub()` `:2616` のシグネチャは `cover_bg: str = "#16234b"` を
`cover_bg: str | None = None` に変え、冒頭で

```python
cover_bg = _resolve_cover_bg(cover_bg, meta, site_name)
```

を1行呼ぶ。**17サイトの `build_epub(..., cover_bg=args.cover_bg, ...)` はそのままでよい。**

### 3.2 変更点一覧（A）

| # | 箇所 | 変更 |
|---|---|---|
| A-1 | `_META_FIELDS` `:447` | `("theme_color", "テーマカラー", "str")` を末尾に追加 |
| A-2 | `_SITE_DISPATCH` `:9914` 直後 | `_SITE_COLOR_BY_LABEL` と `_resolve_cover_bg()` を新設 |
| A-3 | `build_epub()` `:2616` | 既定値を `None` に変更し、冒頭で `_resolve_cover_bg()` を呼ぶ |
| A-4 | `_main()` `:11006-11008` | サイト既定色の代入を**削除**（None のまま通す） |
| A-5 | `_main()` `:10946-10947` | `--from-file` の `"#16234b"` 代入を**削除** |
| A-6 | `run_from_file()` `:8849` | `cover_bg = args.cover_bg or "#16234b"` → `args.cover_bg` をそのまま渡す |
| A-7 | `_append_one()` `:10078` | `getattr(base_args,"cover_bg",None) or default_color` → `or` 以降を削除 |
| A-8 | `run_watch()` `:10476` | `cover_bg=default_color` → `cover_bg=getattr(args,"cover_bg",None)` |
| A-9 | 新設 | `_rel_luminance()` / `_contrast_ratio()` / `_clamp_cover_bg()` / `_cover_palette()`（§3.3.5） |
| A-10 | `make_cover_image()` `:2461-2585` | §3.3.5 の表に従って配色・縁取り・スクリムを差し替え。**影の 2 行は削除**（§3.3.3 のバグ） |
| A-11 | `_make_cover_svg()` `:2383` | `paint-order="stroke"` で同等の縁取りを入れる（§3.3.6） |
| A-12 | `wrap_text()` `:2524` の `max_title_w` | 縁取りの太り分 `stroke_width * 2` を引く（§3.3.5 の注記） |

**A-1 の副次効果（意図した挙動）**

- `.txt` ヘッダーに `テーマカラー：#42B8C1` が出るので、`--from-file` / `--append` で
  ePub を作り直しても色が維持される（`_parse_meta_lines` が自動で読む）
- `_nd_meta_pairs()` `:462` が `_META_FIELDS` を走査するため、OPF に
  `<meta property="nd:themeColor">#42B8C1</meta>` が**自動で出る**。`nd:` プレフィックスは宣言済みなので
  epubcheck は通るはずだが、**metadata を触るので §6 の epubcheck 実行は必須**

**A-4/A-5/A-6 の副次効果（意図した挙動）**

`--from-file` は現在 `site_name` に関係なく常に `#16234b` を使っている。A 適用後は
`.txt` の `配信元：カクヨム` から逆引きした既定色（`#4BAAE0`）、テーマカラー行があればその色になる。
**`--from-file` の出力色が変わる**が、これは「サイトから落とした ePub と再生成した ePub の色が揃う」
という改善であり、意図した変更。

### 3.3 視認性の確保（複合対策）

> **初版の設計を実測で否定した。** 当初は「HSV の V を 0.62 で clamp する」だけで足りると書いたが、
> 実際にレンダリングして WCAG コントラスト比を測ったところ**明度クランプ単独では不足**だった。
> 相対輝度は G 成分の寄与が 0.7152 と支配的なため、**緑〜黄緑〜水色系は V を落としても輝度が下がらない**。

#### 3.3.1 実測：クランプ単独では AA(4.5) に届かない

背景 = クランプ後の色、文字 = 現行のクリーム `(255,248,215)` で測ったコントラスト比。

| カクヨムの色 | 元 | V=0.62 クランプ後 | クリームとの比 | 判定 |
|---|---|---|---|---|
| 黄緑 `#BAC23E` | V 0.76 | `#979E32` | **2.71** | ✗ 不可 |
| 水色 `#42B8C1` | V 0.76 | `#36969E` | **3.27** | ✗ 不可 |
| 緑 `#509600` | V 0.59 | 変化なし | **3.45** | ✗ 不可 |
| 橙 `#FF9D00` | V 1.00 | `#9E6100` | 4.72 | ○ |
| 桜 `#FF8A94` | V 1.00 | `#9E555B` | 5.04 | ○ |
| 桃 `#FF5D81` | V 1.00 | `#9E394F` | 6.24 | ○ |
| 黒 `#000000` | V 0.00 | `#1E1E1E` | 15.61 | ○ |

黄緑の実レンダリング（`cov_yellowgreen_clamp.jpg`）を目視しても、オリーブ地にクリーム文字で
**明確に読みにくい**。クランプを更に強めれば解決するが、それは**作者の選んだ色を潰す**方向であり
A の目的に反する。→ **他の手段と組み合わせる。**

#### 3.3.2 使える手段の棚卸し

| # | 手段 | 効果 | 実装コスト | 色の保存 |
|---|---|---|---|---|
| **V1** | **文字の縁取り**（`stroke_width` / `stroke_fill`） | **最大**。背景色に依存せず効く | 小（Pillow 標準機能） | **完全** |
| **V2** | **文字色の明暗切り替え**（クリーム ⇄ 濃墨） | 大 | 小 | **完全** |
| **V3** | スクリム（タイトル帯に半透明の板を敷く） | 中〜大 | 中（RGBA 合成が要る） | 部分 |
| **V4** | 枠線・罫線の色を背景に追随させる | 小（枠の視認性のみ） | 小 | — |
| **V5** | 影を本当に半透明にする（**現行はバグ**） | 小〜中 | 中（RGBA 合成） | — |
| **V6** | 明度クランプ（初版案） | 中 | 小 | **失われる** |

**V1 が最も強い。** Pillow 10.2.0 で `stroke_width` / `stroke_fill` が使えることを実機確認済み。
グリフの周囲に縁取りが付くので、**背景がどんな色でも文字の輪郭が立つ**。
しかも背景色を一切変えないため、作者の選んだ色をそのまま出せる。

**V2 は「色を守る」点で V6 より優れている。** 背景を暗くする代わりに文字を濃墨にすれば、
同じ可読性をより明るい背景で達成できる。実測：

| 色 | V6 単独（クランプ 0.62・クリーム） | V2 併用（クランプ 0.72・文字色を選択） |
|---|---|---|
| 黄緑 | `#979E32` / 比 2.71 ✗ | `#B0B73A` + **濃墨** / 比 **8.28** ○ |
| 水色 | `#36969E` / 比 3.27 ✗ | `#3EAFB7` + **濃墨** / 比 **6.86** ○ |
| 橙 | `#9E6100` / 比 4.72 ○ | `#B77100` + **濃墨** / 比 **4.60** ○ |
| 桃 | `#9E394F` / 比 6.24 ○ | `#B7425C` + クリーム / 比 **4.98** ○ |
| 黒 | `#1E1E1E` / 比 15.61 ○ | `#1E1E1E` + クリーム / 比 **15.61** ○ |

**背景色がより原色に近いまま（`#979E32` → `#B0B73A`）、コントラストは上がる。**

#### 3.3.3 V5：現行の「影」は仕様どおり動いていない（既存バグ）

```python
:2555   draw.text((x+3, y+3), line, font=font_t, fill=(0, 0, 0, 100))   # タイトルの影
:2584   draw.text((ax+2, ay+2), author, font=font_a, fill=(0, 0, 0, 100))  # 著者名の影
```

キャンバスは `Image.new("RGB", ...)` `:2464` で**アルファチャンネルを持たない**。
RGB 画像に 4要素タプルを渡すと **Pillow はアルファを黙って捨てる**。実測で確認した。

```
RGB 画像に fill=(0,0,0,100) で描画 → グリフ最暗画素 = (0,0,0)
RGB 画像に fill=(0,0,0)     で描画 → グリフ最暗画素 = (0,0,0)   ← 同一
```

つまり「不透明度 39% のやわらかい影」のつもりが、**+3px ずらした真っ黒のコピー**になっている。
現状の可読性はこの濃い影に支えられている面がある。**V1 の縁取りに置き換えれば影は不要**なので、
本設計では**影を廃してストロークに一本化する**（RGBA 合成を増やさずに済み、実装も単純になる）。

#### 3.3.4 決定：縁取りは使わない。題簽パネルで解決する

> **V1（縁取り）は採用しない。** プロトタイプを見た結果、**「チカチカして目の負担になる」**という
> 評価だったため取り下げる。数値上は最も強力だが、明暗の細かい境界が字画ごとに生じるので
> 長い日本語タイトルでは視覚的ノイズになる。§3.3.2 の棚卸しでは「効果=最大」としたが、
> **効果と快適さは別**だった。

代わりに §3.5 で採用する**題簽（だいせん）パネル**が、縁取りの役割をより穏やかに果たす。
タイトルを淡色のベタ面の上に置くので、**背景色が何であってもコントラストが一定**になる。

**適用順（すべて自動判定・新規オプションなし）**

```
Step 1  V6 明度クランプ                     V ∈ [0.12, 0.46]
        → 地は「濃い色の紙」に寄せる。題簽が主役なので地は沈めてよい
Step 2  V2 地に直接置く文字の色を選ぶ         クリーム(255,248,215) か 濃墨(28,22,12)
        → contrast(色, 地) が大きいほうを採用。対象は著者名と配信元のみ
Step 3  題簽パネル上の題名は固定配色           紙 (247,243,231) ＋ 濃墨 (26,20,12)
        → 比 16.47 で常に一定。背景色に一切依存しない
```

**V3（スクリム）は題簽パネルに吸収されたため不要。V1（縁取り）は全廃。**

**実測結果**

| 色 | 現行 | 縁取り案（不採用） | **採用案（題簽）** |
|---|---|---|---|
| | 題名 | 題名 | 題名（題簽上） / 著者名（地の上） |
| 橙 `#FF9D00` | 1.95 ✗ | 4.60 | **16.47** / 7.33 |
| 黄緑 `#BAC23E` | 1.81 ✗ | 8.28 | **16.47** / 4.61 |
| 水色 `#42B8C1` | 2.22 ✗ | 6.86 | **16.47** / 5.43 |
| 桜 `#FF8A94` | 2.11 ✗ | 4.28 | **16.47** / 7.71 |
| 黒 `#000000` | 19.66 ○ | 15.61 | **16.47** / 15.61 |
| なろう既定 `#18b7cd` | — | — | **16.47** / 6.04 |

**題名は全色で 16.47 の一定値**、地に置く著者名も**全色 4.61 以上**で WCAG AA を満たす。
クランプ上限を 0.46 にしたのはこの 4.5 ラインを全色でクリアさせるため
（0.55 では水色が 4.15、0.50 では黄緑が 4.18 と割り込む。実測で決めた）。

#### 3.3.5 実装スケッチ（`stroke_*` を含まない最終形）

```python
_COVER_V_MAX   = 0.46          # 地を沈める。実測で全色 AA を満たす上限（§3.3.4）
_COVER_V_MIN   = 0.12          # 真っ黒防止
_COVER_INK_LIGHT = (255, 248, 215)   # 現行のクリーム
_COVER_INK_DARK  = (28, 22, 12)      # 明るい地用の濃墨
_COVER_PANEL     = (247, 243, 231)   # 題簽の紙色（背景色によらず固定）
_COVER_PANEL_INK = (26, 20, 12)      # 題簽上の題名（同上）
_COVER_PANEL_RULE = (150, 134, 96)   # 題簽の内側罫

def _rel_luminance(rgb: tuple) -> float:
    """WCAG 2.x の相対輝度。"""
    def _lin(c):
        c /= 255
        return c / 12.92 if c <= 0.03928 else ((c + 0.055) / 1.055) ** 2.4
    r, g, b = (_lin(c) for c in rgb)
    return 0.2126 * r + 0.7152 * g + 0.0722 * b

def _contrast_ratio(a: tuple, b: tuple) -> float:
    la, lb = _rel_luminance(a), _rel_luminance(b)
    hi, lo = max(la, lb), min(la, lb)
    return (hi + 0.05) / (lo + 0.05)

def _clamp_cover_bg(hex_str: str) -> str:
    """背景色の明度だけを可読域に収める。色相・彩度は変えない。"""
    r, g, b = _parse_hex_color(hex_str)
    h, s, v = colorsys.rgb_to_hsv(r / 255, g / 255, b / 255)
    v2 = min(max(v, _COVER_V_MIN), _COVER_V_MAX)
    if abs(v2 - v) < 1e-6:
        return hex_str
    r2, g2, b2 = colorsys.hsv_to_rgb(h, s, v2)
    return "#%02X%02X%02X" % (int(r2 * 255), int(g2 * 255), int(b2 * 255))

def _cover_bg_ink(cover_bg: str) -> tuple:
    """地の上に直接置く文字（著者名・配信元）の色を決める。縁取りは使わない。"""
    bg = _parse_hex_color(cover_bg)
    if _contrast_ratio(_COVER_INK_LIGHT, bg) >= _contrast_ratio(_COVER_INK_DARK, bg):
        return _COVER_INK_LIGHT
    return _COVER_INK_DARK
```

**題名の配色は関数を必要としない**（題簽の紙色と濃墨の固定2色）。
`stroke_width` / `stroke_fill` / スクリムは**一切使わない**。
`make_cover_image()` の描画は §3.5.3 の仕様に沿って全面的に書き直す。

#### 3.3.6 SVG フォールバック

`_make_cover_svg()` `:2383` は Pillow 不在時の経路。題簽は `<rect>` ＋ `<text>` で素直に書ける。
縁取り（`paint-order="stroke"`）は**使わない**ので、Pillow 版と同じく
「淡色の矩形の上に濃墨の縦組みテキスト」を出すだけでよい。

> SVG の縦組みは `writing-mode: vertical-rl` で指定する。ただし SVG 表紙を正しく描ける
> リーダーは限られる（既存コードにも警告 `:2601` がある）ため、**ここは凝らない**方針とする。

#### 3.3.7 既存17サイトの既定色への影響

複合対策は `theme_color` 由来かどうかに関係なく**全サイトの既定色にも適用される**。
明るい既定色（`#FA8296` 野いちご V=0.98、`#E4097D` ステキブンゲイ V=0.89、
`#0CBF97` ノベルアップ＋ V=0.75、`#18b7cd` なろう V=0.80）は
**文字が濃墨に切り替わる**可能性が高く、見た目の変化は小さくない。

ただし初版案（V を 0.62 まで落とす）と比べれば**背景色の変化はずっと小さい**（上限 0.72・
文字色側で吸収するため）。「色は保ったまま文字が読めるようになる」方向なので、
**全サイトに一律適用する**方針とする。実装後に17色すべてのビフォー／アフターを目視確認すること。

---

## 3.5 装飾（「表紙らしさ」の強化）

現行の自動生成表紙は「グラデーション＋細い二重枠＋中央にタイトル」だけで、書影というより
プレースホルダに見える。§3.3 の可読性対策と同時に意匠も入れる。**新規の外部依存は増やさない**
（Pillow だけで完結・オフライン動作・出力は決定的）。

### 3.5.1 使える素材の棚卸し

意匠に使える情報は**すでに meta で持っている**。

| 素材 | 出所 | 取得できるサイト |
|---|---|---|
| キャッチコピー | `meta["catchphrase"]` | **カクヨム**（`_kky_meta_from_work` `:3769`）、**エブリスタ**（`_est_meta_from_page` `:3598`）の2サイトのみ |
| 配信元 | `meta["site"]` | 全サイト |
| ジャンル | `meta["genre"]` | 大半 |
| 話数・連載状態 | `meta["episode_count"]` / `["serial_status"]` | 大半 |

→ **帯の文言は3段で退避させる**（`catchphrase` → `ジャンル　全N話　状態` → `配信元` のみ）。
カクヨム／エブリスタ以外でも帯が空にならない。

### 3.5.2 Pillow で使える技法（実機確認済み）

| 技法 | 実装 | 確認結果 |
|---|---|---|
| **縦組みタイトル** | `draw.text(..., direction="ttb")` | **raqm 有効を確認**（`features.check("raqm")` → True）。`「」` の回転と長音符 `ー` の縦字形が**自動で正しく出る**。手動の字形テーブルは不要 |
| 紙のざらつき | `Image.effect_noise()` を低不透明度で合成 | 平板さが消える |
| ビネット（周辺減光） | `ImageDraw.ellipse` + `GaussianBlur` をマスクに `Image.composite` | 中央に視線が寄る |
| 地紋（青海波） | `draw.arc` を格子状に、α 20 前後 | 和書の質感 |
| 帯（obi） | 下部 17〜19% に半透明の帯 ＋ 上辺に差し色の罫 | **最も「本らしさ」に効く** |
| 題簽パネル | 角丸なしの矩形＋内側罫＋ドロップシャドウ | 和装本の題簽 |
| 隅の飾り罫 | 四隅の L 字線 | 安価に締まる |

> **縦組みが使えるのは大きい。** 本文が縦書き ePub なのに表紙だけ横組みという不整合も解消する。

### 3.5.3 採用：案2「題簽（和装本風）」— 確定仕様

3案（案1 文庫カバー／案2 題簽／案3 現行改良）を描き起こして比較し、**案2 を採用**した。
選定理由は「和装本風が最も見やすい」という評価と、§3.3.4 のとおり**題簽パネルが
背景色に依存しない一定のコントラストを与える**という構造的な利点による。

プロトタイプ: `scratchpad/daisen.py`、比較シート `scratchpad/covers.html`、
出力 `d2_{色}.jpg`。

**キャンバス** `_COVER_W, _COVER_H = 800, 1200`（現行のまま）

| 要素 | 仕様 |
|---|---|
| 地 | `_clamp_cover_bg()`（V ≤ 0.46）した色で上→下グラデーション（上端は `_darken_color` 0.6倍） |
| 地紋 | 青海波。`draw.arc` を半径 96 の千鳥格子で3重、α=20。地が暗ければ白・明るければ黒 |
| 紙のざらつき | `Image.effect_noise((W,H), 5)` を 0.30 でブレンド |
| ビネット | 楕円マスク＋`GaussianBlur(120)`、強度 70 |
| **題簽パネル** | **幅 `W*0.60` = 480px**・高さ `H*0.72` = 864px。右寄せ（右余白 62px）・上 `H*0.085` |
| 題簽の影 | パネルを (+9, +11) ずらして α=70 の黒矩形 |
| 題簽の紙 | `(247,243,231)` 固定。内側 12px に `(150,134,96)` の罫 2px |
| **題名** | 縦組み `direction="ttb"`・`(26,20,12)` 固定・**縁取りなし**。サイズは §3.5.3.1 で自動決定 |
| 著者名 | 題簽の左（`PX-54`）に縦組み・44px・`_cover_bg_ink()` の色 |
| 配信元 | 著者名の下に縦組み・27px・同色 |

#### 3.5.3.1 題名のサイズ決定（題簽幅を活かす要）

**初版プロトタイプは題簽が 250px 幅で、題字が 47px と小さすぎた。**
幅を 480px（**約1.9倍**）にしたことで題字は **82px**（長いタイトル）〜**132px**（短いタイトル）まで
拡大した。アルゴリズムは「**内寸に収まる最大サイズを上から探す**」。

```python
def _fit_panel_title(title, inner_w, inner_h, col_gap=1.22, start=132):
    """題簽の内寸に収まる最大の字サイズ・1列字数・列数を返す。"""
    sz = start
    while sz > 26:
        per  = max(1, int(inner_h // (sz * 1.06)))     # 1列に入る字数
        ncol = math.ceil(len(title) / per)             # 必要な列数
        if ncol * (sz * col_gap) <= inner_w:           # 幅に収まるか
            return sz, per, ncol
        sz -= 2
    per = max(1, int(inner_h // (26 * 1.06)))
    return 26, per, math.ceil(len(title) / per)
```

`inner_w = PW - 74`、`inner_h = PH - 78`。決定後に**2つの調整を必ず入れる**。

1. **列あたり字数を均等配分し直す** — `per = math.ceil(len(title) / ncol)`。
   これをやらないと最終列だけ極端に短くなり、題簽の下半分が空いて間延びする
   （30字・4列なら 9/9/9/3 → **8/8/8/6**）
2. **題簽内で縦中央寄せ** — `y_top = PY + max(38, (PH - per*sz*1.06) // 2)`。
   短いタイトルでも上に寄らない

**実測結果**

| ケース | 字数 | 題字サイズ | 列数 |
|---|---|---|---|
| カクヨムの長題（`ハズレスキル「小箱」　〜異世界でも…`） | 30 | **82px** | 4 |
| 中くらい（`転生したら剣でした`） | 9 | **132px** | 2 |
| 短題（`走れメロス`） | 5 | **132px** | 1 |

> `start=132` が上限。これ以上大きくすると 1〜2字のタイトルで字が題簽からはみ出す。

### 3.5.4 縮小表示での見え方（実測）

書棚一覧では表紙は 1/8 程度に縮む。**縁取り案**（不採用）の字面領域を測ったところ

```
原寸       墨 35,423 px / 縁 40,590 px   ← 縁のほうが面積が広い
1/8 縮小   中間 3,637 px / 暗 142 px     ← 墨と縁が混ざって中間調に潰れる
```

**縁取りは原寸では効くがサムネイルでは墨と混ざって消える**うえ、原寸では
明暗の細かい境界が字画ごとに出て**チカチカする**。これが §3.3.4 で縁取りを取り下げた
2つ目の根拠になった。

採用案（題簽）はこの問題を持たない。**淡色のベタ面と濃墨という大きな面積のコントラスト**なので、
縮小しても関係が崩れず、原寸でも境界は題簽の輪郭 1本だけで済む。

### 3.5.5 実装上の注意（プロトタイプで踏んだもの）

1. **隅の飾り罫は枠矩形の内側に閉じ込める。** 素直に `H-M` を使うと帯の下に潜り込んで
   途中で切れた L 字が見える。枠の下辺（＝帯の上端）を渡すこと
2. **題簽パネルは「列数 × 列送り ≤ パネル内寸」を満たすまでフォントを縮める。**
   高さだけで判定すると横にはみ出す（カクヨムの30字超のタイトルで再現）
3. **`direction="ttb"` は raqm 必須。** `PIL.features.check("raqm")` が False の環境では
   横組みへ退避する分岐が要る（Pillow 自体が無い場合は既存の SVG フォールバックへ）
4. 帯・スクリムの半透明合成は `ImageDraw.Draw(img, "RGBA")` を使えば
   `alpha_composite` の往復なしで描ける

### 3.5.6 帯（obi）は採用案では使わない

§3.5.1 でキャッチコピーを帯に出す案を検討したが、**採用した案2 には帯がない**（案1・案3 の要素だった）。
`meta["catchphrase"]` は引き続き `.txt` ヘッダーと OPF の副題（`title-type=subtitle`）に出るので、
情報としては失われない。表紙に帯を足すかどうかは**将来の追加検討**とする。

配信元（`meta["site"]`）は著者名の下に縦組みで小さく入れるので、案2 でも表紙に残る。

### 3.5.7 残る検討事項

- **`--cover-style` を設けるか**: 案1・案3 のコードはプロトタイプとして `cover3.py` に残っている。
  まず案2 単独で実装し、**要望が出たら追加する**方針（先にオプションを増やさない）
- **青海波の地紋を全サイトに出してよいか**: 現代小説の表紙としては和風に寄りすぎる可能性がある。
  α=20 と十分薄いが、実装後に何作品か作って判断する。無効化は定数1つで済むようにしておく

---

## 4. 設計 C — `--use-site-cover` の品質ゲート

### 4.1 決定：3段構えにする

現状は「og:image が取れたら無条件採用 / 取れなければ自動生成」の2分岐。これを

```
1. サイト固有の高解像度表紙 URL を試す   （berry's / ノベマ！の -thumb 除去）
2. og:image を品質ゲートに通す           （通らなければ不採用）
3. 自動生成表紙へフォールバック           （A のテーマカラー付き）
```

に変える。**§1.2 で ✗ を付けた8サイトはゲートで自動的に落ちる。**

### 4.2 品質ゲートの判定条件

| # | 条件 | 落ちるサイト | 根拠 |
|---|---|---|---|
| G1 | URL にプレースホルダ語が含まれる（`no_image` / `/common/` / `top_logo`） | ノベルアップ＋、青空文庫、アルファポリス（表紙未設定作品） | §1.2 の URL 実測 |
| G2 | **横長または正方形**（`width >= height`） | なろう、カクヨム、ハーメルン、ソリスピア、ネオページ、エブリスタ、monogatary | 表紙は縦長。1200×630 を弾く唯一確実な判定 |
| G3 | 小さすぎる（`width < 200`） | berry's の thumb（166px、C-1 で救済されなければ） | 800×1200 へ引き伸ばすと破綻するため |

**閾値 `width < 200` の根拠**: NOVEL DAYS は 200×320 が最大で上位版が存在しない（§1.3）。
ここを 300 にすると NOVEL DAYS が実表紙を失う。**「小さくても本物の表紙を優先する」方針**を取り、
200 を境界（200 は通す）にする。引き伸ばしのボケは許容する。

**G1 は Pillow 不要**（URL 文字列判定）、**G2/G3 は Pillow が要る**。
`_PILLOW_AVAILABLE` `:142` が False の環境では **G1 のみ適用**し、G2/G3 はスキップして従来動作に退避する。
その場合は「Pillow 未導入のため画像寸法を検査できません」と1行 stderr に出す。

```python
_COVER_REJECT_URL_PAT = re.compile(r"(no_image|/common/|top_logo)", re.I)
_COVER_MIN_WIDTH = 200

def _cover_image_acceptable(data: bytes, img_url: str) -> tuple[bool, str]:
    """表紙として使える画像か判定する。(可否, 却下理由) を返す。"""
    if _COVER_REJECT_URL_PAT.search(img_url):
        return False, "サイト共通のプレースホルダ画像です"
    if not _PILLOW_AVAILABLE:
        return True, ""
    try:
        im = Image.open(io.BytesIO(data))
        w, h = im.width, im.height
    except Exception:
        return True, ""          # 判定できないなら通す（従来動作）
    if w >= h:
        return False, f"表紙ではなく横長のシェア用画像です（{w}×{h}）"
    if w < _COVER_MIN_WIDTH:
        return False, f"画像が小さすぎます（{w}×{h}）"
    return True, ""
```

却下時は次を stderr に出して `""` を返し、自動生成表紙へ落とす。

```
[情報] サイト公式サムネイル: 表紙ではなく横長のシェア用画像です（1200×630）。
       自動生成の表紙を使用します。
```

> 既存の失敗系メッセージは `[警告]` を使っているが、**ゲートによる却下は異常ではない**ので
> `[情報]` にする。なろう・カクヨムでは毎回出ることになるため、警告にすると常時ノイズになる。

### 4.3 サイト固有の高解像度化

```python
def _site_hires_cover_url(site: str, img_url: str) -> str:
    """サイト固有の高解像度表紙 URL を返す。無ければ ""。"""
    if site in ("berrys", "novema") and "-thumb." in img_url:
        return img_url.replace("-thumb.", ".")
    return ""
```

og:image の URL を得た直後に呼び、返り値があれば**そちらを先に GET する**。
HTTP 200 かつ画像として開ければ採用、失敗したら元の og:image URL に戻す（フォールバック必須）。

`site` は `_fetch_ogp_cover()` の引数として受け取る（§2.4 のとおり両呼び出し箇所で確定済み）。

### 4.4 変更点一覧（C）

| # | 箇所 | 変更 |
|---|---|---|
| C-1 | `_fetch_ogp_cover()` `:9555` | シグネチャを `(page_url: str, site: str = "")` に変更 |
| C-2 | 同 `:9594` 直後 | og:image URL 確定後に `_site_hires_cover_url()` を試す（失敗時は元 URL へ戻す） |
| C-3 | 同 `:9626` 付近 | `img_data` 取得後に `_cover_image_acceptable()` を通し、却下なら `""` を返す |
| C-4 | 新設 | `_COVER_REJECT_URL_PAT` / `_COVER_MIN_WIDTH` / `_cover_image_acceptable()` / `_site_hires_cover_url()` |
| C-5 | `_main()` `:11013` | `_fetch_ogp_cover(args.url)` → `_fetch_ogp_cover(args.url, site)` |
| C-6 | `_append_one()` `:10090` | `_fetch_ogp_cover(url)` → `_fetch_ogp_cover(url, site)` |

**import の確認済み**: `io` は `:100` で import 済み（追加不要）。`colorsys`（§3.3 で使用）は
未 import なので `:108` の `import unicodedata` の隣に `import colorsys` を足す。どちらも stdlib。

---

## 5. 適用後に各サイトがどうなるか（`--use-site-cover` 指定時）

| サイト | 現在 | 適用後 |
|---|---|---|
| なろう | 横長カード（破綻） | 自動生成（サイト既定色 `#18b7cd`） |
| **カクヨム** | 横長カード（破綻） | **自動生成（作品の `baseColor`）** ← A と C の両方が効く |
| ハーメルン / ソリスピア / ネオページ / エブリスタ | 横長カード（破綻） | 自動生成（サイト既定色） |
| ノベルアップ＋ | サイト共通画像 | 自動生成（サイト既定色） |
| 青空文庫 | サイトロゴ | 自動生成（`#000066`） |
| アルファポリス | 表紙 or ジャンル別プレースホルダ | 表紙あり→そのまま／プレースホルダ→自動生成 |
| monogatary | 正方形 750×750 | 自動生成（`#231815`） |
| 野いちご / ステキブンゲイ | 実表紙 | **変化なし**（実表紙のまま） |
| **berry's cafe** | 166×250 | **500×750**（-thumb 除去） |
| **ノベマ！** | 250×342 | **500×684**（-thumb 除去） |
| NOVEL DAYS | 200×320 | 変化なし（200 は通す） |
| 杉田玄白 / 結城浩 | og:image なし → 自動生成 | 変化なし |

`--use-site-cover` **なし**の場合は A のみが効き、カクヨムの自動生成表紙が `baseColor` になる。

---

## 6. 検証手順

```bash
# 構文チェック
python -m py_compile novel_downloader.py

# A: カクヨムのテーマカラーが表紙に乗るか（--use-site-cover なし）
python novel_downloader.py https://kakuyomu.jp/works/822139846234329769 --end 1 -o t_kky
#   → .txt ヘッダーに「テーマカラー：#42B8C1」が出ること
#   → 表紙が #36969E 系（クランプ後）のグラデーションになること

# A: .txt 経由でも色が維持されるか
python novel_downloader.py --from-file t_kky.txt -o t_kky_ref
#   → t_kky.epub と t_kky_ref.epub の表紙が同色であること

# C: 横長カードが弾かれるか（なろう・カクヨム）
python novel_downloader.py https://ncode.syosetu.com/n0022gd/ --end 1 --use-site-cover -o t_narou
#   → 「[情報] …横長のシェア用画像です（1200×630）」が出て自動生成に落ちること

# C: 実表紙が残るか（野いちご・ステキブンゲイ）
python novel_downloader.py https://www.no-ichigo.jp/book/n1179797 --end 1 --use-site-cover -o t_ni
#   → 750×1102 の実表紙がそのまま入ること

# C: 高解像度化（ノベマ！・berry's）
python novel_downloader.py https://novema.jp/book/n1640364 --end 1 --use-site-cover -o t_nv
#   → OEBPS/images/cover.jpg が 500×684 であること

# epubcheck（A-1 で OPF に nd:themeColor が増えるため必須）
java -jar /home/ayati/epubcheck-5.3.0/epubcheck.jar t_kky.epub

# 表紙の寸法確認
python -c "import zipfile,io;from PIL import Image;z=zipfile.ZipFile('t_nv.epub');print(Image.open(io.BytesIO(z.read('OEBPS/images/cover.jpg'))).size)"
```

**回帰確認**: `--append` / `--append-dir` / `--watch` は `_append_one()` を通るので、
A-7 / A-8 / C-6 の3点が入っているか実行して確かめる（`cover_bg` が None のまま渡って
`build_epub()` で解決されること）。

**コントラスト比の自動確認**: §3.3 の複合対策は数値で検証できる。実装後に次を回し、
**全色で 4.5 以上**（大文字サイズ基準なら 3.0 以上）になることを確かめる。

```bash
python3 - <<'EOF'
import novel_downloader as nd
# カクヨム実測54色のうち代表7色 ＋ 17サイトの既定色すべて
colors = ["#FF9D00","#BAC23E","#42B8C1","#FF8A94","#FF5D81","#509600","#000000"] \
       + [c for _l, c, _f in nd._SITE_DISPATCH.values()]
for c in colors:
    bg  = nd._parse_hex_color(nd._clamp_cover_bg(c))
    pal = nd._cover_palette(nd._clamp_cover_bg(c))
    r   = nd._contrast_ratio(pal["ink"], bg)
    print(f"{c}  比={r:5.2f}  {'OK' if r >= 4.5 else 'NG'}  "
          f"文字={'クリーム' if pal['ink'][0] > 128 else '濃墨'}  "
          f"スクリム={'あり' if pal['scrim'] else 'なし'}")
EOF
```

**目視確認**: 数値が通っても縁取りの太さと折り返しは目で見ないと分からない。実装後、
明るい既定色（`#FA8296` 野いちご、`#E4097D` ステキブンゲイ、`#0CBF97` ノベルアップ＋、
`#18b7cd` なろう）と、文字色が切り替わる境界付近の色で表紙を並べてビフォー／アフターを見る。
特に **A-12（`max_title_w` の縁取り分の減算）** を入れ忘れると右端が枠に食い込むので、
**長いタイトル**（例: `ハズレスキル「小箱」　〜異世界でも四畳半あれば生きていける〜`）で確認する。

参考として、調査時に作ったプロトタイプの比較画像が
`/tmp/claude-1000/-home-ayati-novel-downloader/*/scratchpad/cov_{色}_{current,clamp,combo}.jpg`
に残っている（セッション用の一時領域なので消えていれば §3.3.5 のスケッチから再生成する）。

---

## 7. 見送った案 B（ジャンル別の既定色）

`_normalize_genre()` の共通ジャンル10種に色を割り当て、テーマカラーを持たない16サイトの
既定色に使う案。**見送る。** 理由は次の2点。

1. **ジャンルでの絞り込みは yomikake の読みかけ一覧で既にできる**ので、表紙の色で
   ジャンルを表現する必要がない
2. アルファポリスが実際にやっている（`no_image/novel/fantasy.png`）が、**アイコンの色と
   ジャンルの対応に納得感がない**。一次情報として参照する価値が薄い

「作者の選んだ色を尊重する」という A の目的に対して、ジャンル色は
**サイトでも作者でもない第三者（このツール）が決めた色**であり、目的からずれている。

---

## 8. CLAUDE.md への反映

反映済み（実装と同時）。

- 「主な規約 → 表紙背景色のデフォルト」に優先順位と明度クランプ・題簽意匠を追記
- 「オプション一覧」の `--use-site-cover` に品質ゲートの説明を追記
- 「アーキテクチャ 2」に題簽レンダラ一式を追記
- 「アーキテクチャ 6b」の `_META_FIELDS` に `theme_color` を追記
- 「アーキテクチャ 7」に `_SITE_COLOR_BY_LABEL` / `_resolve_cover_bg()` を追記

---

## 9. 実装結果（2026-07-28）

### 9.1 設計との差分（実装中に判明して追加したもの）

| # | 事象 | 対処 |
|---|---|---|
| **9-A** | **欧文の著者名が 1 文字ずつ縦に積まれて読めない。** カクヨムの実作品で著者が `@tea_kaijyu` だったため発覚（`PJ666` などサイト上に多数いる）。設計時は日本語の筆名しか想定していなかった | `_RE_HAS_CJK` で CJK を含まない文字列を判定し、**時計回りに 90 度倒して描く**（縦組みにおける欧文の作法）。`_draw_vtext_auto()` を新設。サイト名 `berry's cafe` / `NOVEL DAYS` / `monogatary.com` にも同じ経路が効く |
| **9-B** | monogatary の og:image は 750×750 の**正方形**なのに「横長のシェア用画像です」と表示された | `w > h`（横長）と `w == h`（正方形）でメッセージを分けた |
| **9-C** | `_cover_bg_ink()` の**濃墨の枝が到達しない**。`_COVER_V_MAX = 0.46` で地が必ず暗くなるため、常にクリームが選ばれる | 削除せず残し、**到達しない旨を docstring に明記**した。`_COVER_V_MAX` を上げたときに自動追随させるため |
| **9-D** | `math` が未 import だった（`_fit_panel_title` の `math.ceil` で必要） | `import math` を追加。`colorsys` も同様に追加 |
| **9-E** | `ImageFilter` / `ImageChops` が未 import だった（ビネット・紙ノイズで必要） | Pillow の import 行に追加 |

`make_cover_image()` は巨大な内部関数の塊だったので、**題簽の描画を `_make_cover_daisen()` に
切り出し**、`make_cover_image()` は「Pillow があれば `_make_cover_daisen`、なければ SVG」の
振り分けだけに縮めた。`_cover_gradient` / `_cover_paper_noise` / `_cover_vignette` /
`_cover_seigaiha` / `_cover_font` / `_draw_vtext` / `_draw_vtext_auto` に分割済み。

### 9.2 検証結果

**コントラスト（§6 のスクリプト）— カクヨム代表 7 色 ＋ 17 サイト既定色の計 24 色**

```
NG 件数: 0 / 24
地の文字比 4.61（#BAC23E 黄緑）〜 16.50（#000066 青空文庫）
題簽内は全色 16.47（固定 2 色なので当然一定）
```

**epubcheck — 3 ファイルすべて `0 fatals / 0 errors / 0 warnings / 0 infos`**
（`nd:themeColor` を OPF に追加したが問題なし）

**A の優先順位**

| 条件 | 実測した地の色 |
|---|---|
| `--cover-bg #8B0000` 明示 | `(91,19,20)` 赤系 ← 明示が最優先 |
| 無指定（`theme_color=#42B8C1`） | `(44,87,93)` 青緑系 ← テーマカラー |
| `.txt` ヘッダー | `テーマカラー：#42B8C1` が出力される |
| OPF | `<meta property="nd:themeColor">#42B8C1</meta>` |
| `--from-file` 往復 | 表紙 JPEG の **sha256 が完全一致**（色が維持される） |

**C の品質ゲート — 12 サイトを実 URL で通した結果、全て設計どおり**

| 判定 | サイトと実測値 |
|---|---|
| ✅ 採用（**高解像度化**） | ノベマ！ 250×342 → **500×684** / berry's 166×250 → **500×750** |
| ✅ 採用（そのまま） | 野いちご 750×1102、ステキブンゲイ 830×1080、NOVEL DAYS 200×320 |
| ⛔ 却下（横長） | カクヨム 1200×630、ソリスピア 1200×630、エブリスタ 600×314、なろう 1200×630 |
| ⛔ 却下（正方形） | monogatary 750×750 |
| ⛔ 却下（プレースホルダ） | ノベルアップ＋、青空文庫、アルファポリス |

**回帰**: `--dry-run` / `--check-update` / `--from-file` / `--cover-bg` 明示指定、
SVG フォールバック（`_make_cover_svg` の出力が XML として妥当）をいずれも確認済み。

### 9.3 積み残し

- `_check_update_one()` `:10095` と `_check_update_url()` `:10347` は `cover_bg=default_color` の
  ままだが、**どちらも `no_epub=True` で表紙を作らない**ため実害はない（設計どおり据え置き）
- `--append` / `--append-dir` / `--watch` の実ダウンロード経路（`_append_one`）は
  `cover_bg=None` に変更済みだが、**長時間かかるため実 URL での通し確認は未実施**。
  解決ロジックは `--from-file` と同一（`build_epub` 内の `_resolve_cover_bg`）で、
  そちらは sha256 一致まで確認できている
- 青海波の地紋は `_COVER_SEIGAIHA = True` の 1 定数で無効化できる（§3.5.7）

---

## 10. 追補：エブリスタの画像対応（2026-07-28・実機テストで発覚）

### 10.1 症状と原因

実機テストで「第1話が `！` になり記号と URL が出る」と報告された（`estar.jp/novels/26146379`）。

**原因**: エブリスタの本文は API から **Markdown 画像記法 `![alt](url)`** で返る。
`run_estar` は本文に `normalize_tate()` をかけており、その `_TATE_TABLE` が
`!` → `！` を行うため、`![uuid](https://...)` が `！[uuid](https://...)` に化けて
生の URL が本文に残っていた。

### 10.2 対処：図タグへ変換して ePub に埋め込む

リンク生成ではなく**画像そのものを埋め込む**方が読みやすいので、既存の青空文庫図タグ
（`_FIG_PLAIN_RE` `:1447` ／ `build_epub(images=...)` ／ OPF の `inline_images`）に載せた。
青空文庫の挿絵で使っている経路をそのまま流用でき、新しい仕組みは要らない。

- `_EST_MD_IMG_RE` / `est_extract_images()` を新設
- **`normalize_tate()` より前**に適用する（後だと `!` が化けて手遅れ）
- 変換後: `［＃「挿絵」の図（{uuid}.jpg）入る］`
- 取得失敗した画像はタグごと削除（壊れた `img src` を ePub に残さない）

### 10.3 併せて：エブリスタの公式表紙を発見

「第1話の画像は表紙では？」という指摘を調べた結果、**エブリスタは公式の表紙フィールドを
持っていた**。`__NUXT_DATA__` の `coverImageName` →
`https://img.estar.jp/public/user_upload/{name}`。

| 作品 | 公式表紙（`coverImageName`） | og:image |
|---|---|---|
| 26146379 | **1500×2100** | 600×314（横長カード・§4 のゲートで却下） |
| 26486552 | **930×1692** | 600×314 |
| 26384598 | **1024×1536** | 600×314 |

§4 の候補チェーンに**サイト公式の表紙フィールドを最優先**で追加した
（`_site_cover_url_from_html()`）。これでエブリスタは自動生成表紙ではなく本物の表紙になる。

### 10.4 「第1話の画像＝表紙」は成り立たない

実測して確認した。**作品によって分かれるので、第1話の画像を表紙として扱ってはいけない。**

| 作品 | 第1話の画像 | 公式表紙との関係 |
|---|---|---|
| 26146379 | 1500×2100・ページに画像のみ | **公式表紙と同一**（平均画素差 0.628 ＝ JPEG 再エンコード差だけ） |
| 26486552 | 1639×2048・画像＋キャラ名 | **別画像**（キャラ紹介。第2話も別キャラで、公式表紙は 930×1692 の第3の画像） |

26146379 では表紙と第1話の挿絵が**別ファイルとして二重に格納される**（バイト列が
異なるためハッシュでは重複除去できない）。視覚的にほぼ同一の画像を知覚ハッシュで
消すのは誤検出の危険があるため、**あえて何もしていない**。

### 10.5 検証

- epubcheck: `t_est.epub` / `t_est2.epub` ともに **0 fatals / 0 errors / 0 warnings**
- OPF manifest に `<item id="img-..." media-type="image/jpeg"/>` が自動生成される
- 本文 XHTML: `<img class="illustration" src="images/{uuid}.jpg" alt="挿絵"/>`
- 画像のない作品（26384598）での回帰も確認済み
